---
// src/pages/fr/index.astro
import BaseLayout from "../../layouts/BaseLayout.astro";
import PortfolioCard from "../../components/PortfolioCard.astro";
import BackToTop from "../../components/BackToTop.astro";
---

<BaseLayout
  title="AI Agent Engineer | Systèmes conversationnels en production"
  description="Je construis des agents IA conversationnels avec LangGraph, FastAPI et React. Code propre, testé, documenté. Livré en 4-6 semaines."
  lang="fr"
>
  <!-- Hero Section -->
  <section class="hero">
    <div class="container">
      <div class="hero-content">
        <h1 class="hero-title">Agent Engineer Freelance</h1>
        <p
          class="hero-tagline"
          style="font-size: 1.5rem; font-weight: 600; margin: 1rem 0 1.5rem; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;"
        >
          Agent IA production-ready : pas juste du code, une solution complète
        </p>
        <p class="hero-subtitle">
          Backend + Frontend + Documentation + Tests automatisés. Des agents IA
          maintenables qui réduisent vos coûts et automatisent votre support.
          Livrés en 4-6 semaines.
        </p>
        <div class="tech-badges">
          <span class="badge">Python</span>
          <span class="badge">LangGraph</span>
          <span class="badge">LangChain</span>
          <span class="badge">FastAPI</span>
          <span class="badge">React</span>
          <span class="badge">OpenAI</span>
          <span class="badge">Gemini</span>
          <span class="badge">Claude</span>
          <span class="badge">RAG</span>
          <span class="badge">📚 Doc + Tests inclus</span>
          <span class="badge">Multilingue FR/EN</span>
        </div>
        <div class="hero-cta">
          <a href="#offres" class="btn btn-primary">Voir les offres</a>
        </div>
      </div>

      <div class="chat-preview">
        <div class="chat-header">
          <div class="agent-avatar">🤖</div>
          <div class="agent-info">
            <span class="agent-name">Assistant IA</span>
            <span class="agent-status">En ligne</span>
          </div>
        </div>
        <div class="chat-messages">
          <div class="message user-message">
            <div class="message-avatar">👤</div>
            <div class="message-content">
              Comment authentifier mes requêtes API ?
            </div>
          </div>
          <div class="message agent-message">
            <div class="message-avatar">🤖</div>
            <div class="message-content">
              Pour authentifier vos requêtes, utilisez un Bearer token dans le
              header Authorization.<br /><br />
              <code class="inline-code">Authorization: Bearer YOUR_TOKEN</code
              ><br /><br />
              Vous pouvez générer votre token dans les paramètres de votre compte.
              Le token a une durée de validité de 30 jours.<br /><br />
              Besoin d'aide pour générer votre token ?
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Problématique & Solutions -->
  <section class="context-section section-spacing">
    <div class="container">
      <h2 class="section-title">Pour qui ?</h2>

      <div class="context-grid">
        <div class="context-card">
          <h3>Vous avez ce problème</h3>
          <ul>
            <li>
              Votre équipe technique passe plusieurs heures par semaine sur du
              support répétitif
            </li>
            <li>
              Votre documentation est complète mais personne ne trouve l'info
              rapidement
            </li>
            <li>
              Vous gérez 200+ tickets/mois dont 60% sont des questions
              récurrentes
            </li>
            <li>
              Les chatbots no-code que vous avez testés donnent des réponses
              trop génériques
            </li>
          </ul>
        </div>

        <div class="context-card context-card--solution">
          <h3>Ce que je construis</h3>
          <ul>
            <li>
              <strong>Assistant support technique</strong> : répond aux questions
              sur votre produit, votre API, votre documentation. Sans hallucination
            </li>
            <li>
              <strong>Agent de qualification commerciale</strong> : qualifie les
              leads, collecte les besoins, dirige vers le bon interlocuteur
            </li>
            <li>
              <strong>Assistant onboarding</strong> : guide les nouveaux utilisateurs
              ou collaborateurs, explique les fonctionnalités, réduit le time-to-value
            </li>
            <li>
              <strong>Assistant interne</strong> : RH, IT, compliance, il répond
              aux questions récurrentes de vos équipes
            </li>
          </ul>
        </div>
      </div>

      <div class="context-result">
        <p>
          <strong>Résultat concret :</strong> Les questions récurrentes sont automatisées.
          Votre équipe se concentre sur les cas complexes. Vos utilisateurs ont leurs
          réponses en quelques secondes.
        </p>
      </div>

      <div class="cta-services">
        <p>Vous voulez des exemples concrets pour votre secteur ?</p>
        <a href="/fr/services" class="btn btn-secondary">
          Découvrir tous les cas d'usage détaillés →
        </a>
      </div>
    </div>
  </section>

  <!-- Architecture & Approche -->
  <section class="solution-section section-spacing">
    <div class="container">
      <h2 class="section-title">Comment ça fonctionne ?</h2>

      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon">◆</div>
          <h3>Recherche intelligente dans vos données</h3>
          <p>
            L'agent accède à votre documentation, FAQ, base de connaissances et
            répond avec précision en citant ses sources.
          </p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">◆</div>
          <h3>Intégrations métier</h3>
          <p>
            Connexion à vos outils existants : CRM, ticketing, Slack, base de
            données. L'agent s'intègre dans votre écosystème.
          </p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">◆</div>
          <h3>Fiable et évolutif</h3>
          <p>
            Architecture testée, monitoring en temps réel, documentation
            complète. Prêt pour la production.
          </p>
        </div>
      </div>

      <div class="use-cases">
        <h3>Cas d'usage typiques</h3>
        <ul class="use-cases-list">
          <li>
            Support client automatisé (FAQ, troubleshooting, suivi commandes)
          </li>
          <li>
            Assistant développeur (documentation API, authentification, exemples
            de code)
          </li>
          <li>
            Onboarding utilisateurs (guides interactifs, formation produit)
          </li>
          <li>Support interne (RH, IT, compliance, procédures)</li>
        </ul>
        <p class="metrics-text">
          <strong>Impact mesurable :</strong> Réduction des coûts support • Automatisation
          du support récurrent • Disponible 24/7
        </p>
      </div>
    </div>
  </section>

  <!-- Approche -->
  <section class="approach-section section-spacing">
    <div class="container">
      <h2 class="section-title">Approche</h2>

      <div class="approach-grid">
        <div class="approach-card">
          <h4>Contact direct</h4>
          <p>
            Pas d'intermédiaire. Suivi quotidien de l'avancement. Vous savez
            toujours où vous en êtes.
          </p>
        </div>

        <div class="approach-card">
          <h4>Tests automatisés complets</h4>
          <p>
            Tests unitaires, tests d'intégration, CI/CD. Vous recevez un système
            qui fonctionne et reste maintenable.
          </p>
        </div>

        <div class="approach-card">
          <h4>Documentation complète</h4>
          <p>
            Documentation technique (architecture, APIs, déploiement) ET
            documentation utilisateur. Zéro dépendance : vous comprenez comment
            ça marche, vous pouvez le faire évoluer.
          </p>
        </div>

        <div class="approach-card">
          <h4>Code ownership</h4>
          <p>
            Vous recevez tout le code, commenté, versionné, déployable. Vous en
            êtes propriétaire et restez autonome.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Section Marché -->
  <section class="market-section section-spacing">
    <div class="container">
      <h2 class="section-title">Pourquoi investir maintenant ?</h2>

      <div class="market-stats">
        <div class="stat-card">
          <div class="stat-number">87%</div>
          <p>
            des utilisateurs ont une expérience positive avec les chatbots IA
          </p>
          <span class="stat-source">Source : Botpress 2025</span>
        </div>

        <div class="stat-card">
          <div class="stat-number">25%</div>
          <p>
            de réduction des coûts opérationnels grâce à l'IA conversationnelle
          </p>
          <span class="stat-source">Source : Gartner 2024</span>
        </div>

        <div class="stat-card">
          <div class="stat-number">40%</div>
          <p>
            des entreprises utilisent des chatbots pour le support client 24/7
          </p>
          <span class="stat-source">Source : IBM 2024</span>
        </div>

        <div class="stat-card">
          <div class="stat-number">70%</div>
          <p>des interactions client seront gérées par l'IA d'ici 2028</p>
          <span class="stat-source">Source : Gartner 2024</span>
        </div>
      </div>

      <div class="market-insight">
        <p>
          Le marché des agents conversationnels atteindra <strong
            >50 milliards de dollars en 2030</strong
          >, avec une croissance annuelle de 45%. Les entreprises qui
          investissent maintenant prennent une longueur d'avance sur leurs
          concurrents.
        </p>
        <p class="market-source">
          <em>Sources : GM Insights, Spherical Insights, Dual Media 2025</em>
        </p>
      </div>
    </div>
  </section>

  <!-- Offres -->
  <section id="offres" class="offers-section section-spacing">
    <div class="container">
      <h2 class="section-title">Offres</h2>

      <div class="offers-grid offers-grid--with-poc">
        <!-- POC (Nouveau) -->
        <div class="offer-card offer-card--poc">
          <div class="offer-badge offer-badge--poc">🧪 Validation</div>
          <div class="offer-header">
            <h3>POC</h3>
            <p class="offer-tagline">Audit + Prototype fonctionnel</p>
            <div class="offer-price">5 000 €</div>
            <p class="offer-vat">TVA non applicable, art. 293 B du CGI</p>
          </div>
          <div class="offer-duration">
            ⏱️ Durée approximative : 2-3 semaines
          </div>
          <div class="offer-includes">
            <h4>Livrables</h4>
            <ul>
              <li>Audit documentation complet</li>
              <li>Prototype agent RAG fonctionnel (15-25 pages)</li>
              <li>Tests 20 questions types</li>
              <li>Rapport détaillé (10-15 pages)</li>
              <li>Recommandations stratégiques</li>
              <li>Call restitution (1h30)</li>
              <li>Accès démo 14 jours</li>
            </ul>
          </div>
          <div class="offer-upgrade">
            <p class="offer-upgrade-text">
              <strong>🎁 Upgrade :</strong> 2 500 € déduits si passage au Starter
              ou Scale
            </p>
          </div>
          <div class="offer-use-cases">
            <p>
              <strong>Pour qui :</strong> Premier projet IA, besoin de validation
              technique avant investissement complet
            </p>
          </div>
          <div class="offer-cta">
            <a href="#" class="btn btn-primary">🗓️ Réserver un appel</a>
            <a href="#" class="btn btn-secondary">✉️ Être recontacté</a>
          </div>
        </div>

        <!-- Starter -->
        <div class="offer-card">
          <div class="offer-header">
            <h3>Starter</h3>
            <p class="offer-tagline">Agent conversationnel fonctionnel</p>
            <div class="offer-price">10 000 €</div>
            <p class="offer-vat">TVA non applicable, art. 293 B du CGI</p>
            <p class="offer-after-poc">Ou 7 500 € après POC</p>
          </div>
          <div class="offer-duration">
            ⏱️ Durée approximative : 3-4 semaines
          </div>
          <div class="offer-includes">
            <h4>Livrables</h4>
            <ul>
              <li>Agent conversationnel RAG fonctionnel</li>
              <li>Backend Python + FastAPI</li>
              <li>Frontend React responsive</li>
              <li>Tests unitaires</li>
              <li>Documentation technique</li>
              <li>Déploiement + hébergement</li>
              <li>2 semaines de support</li>
            </ul>
          </div>
          <div class="offer-use-cases">
            <p>
              <strong>Exemples :</strong> FAQ automatisée, assistant docs, pré-qualification
              leads
            </p>
          </div>
          <div class="offer-cta">
            <a href="#" class="btn btn-primary">🗓️ Réserver un appel</a>
            <a href="#" class="btn btn-secondary">✉️ Être recontacté</a>
          </div>
        </div>

        <!-- Scale (Recommandé) -->
        <div class="offer-card offer-recommended">
          <div class="offer-badge">⭐ Recommandé</div>
          <div class="offer-header">
            <h3>Scale</h3>
            <p class="offer-tagline">Système RAG complet</p>
            <div class="offer-price">15 000 €</div>
            <p class="offer-vat">TVA non applicable, art. 293 B du CGI</p>
            <p class="offer-after-poc">Ou 12 500 € après POC</p>
          </div>
          <div class="offer-duration">
            ⏱️ Durée approximative : 4-6 semaines
          </div>
          <div class="offer-includes">
            <h4>Livrables</h4>
            <ul>
              <li>Tout ce qui est dans Starter</li>
              <li>RAG avancé (multi-sources, reranking)</li>
              <li>1-2 intégrations (CRM, API, Slack...)</li>
              <li>Tests automatisés complets (CI/CD)</li>
              <li>Observabilité complète (dashboard)</li>
              <li>Documentation technique + utilisateur</li>
              <li>Formation équipe (2h)</li>
              <li>1 mois de support</li>
            </ul>
          </div>
          <div class="offer-use-cases">
            <p>
              <strong>Exemples :</strong> Support multilingue, onboarding sécurisé,
              assistant compliance
            </p>
          </div>
          <div class="offer-cta">
            <a href="#" class="btn btn-primary">🗓️ Réserver un appel</a>
            <a href="#" class="btn btn-secondary">✉️ Être recontacté</a>
          </div>
        </div>

        <!-- Enterprise -->
        <div class="offer-card">
          <div class="offer-header">
            <h3>Enterprise</h3>
            <p class="offer-tagline">Architecture multi-agents</p>
            <div class="offer-price">30 000 €</div>
            <p class="offer-vat">TVA non applicable, art. 293 B du CGI</p>
          </div>
          <div class="offer-duration">
            ⏱️ Durée approximative : 6-8 semaines
          </div>
          <div class="offer-includes">
            <h4>Livrables</h4>
            <ul>
              <li>Tout ce qui est dans Scale</li>
              <li>Architecture multi-agents avec orchestration</li>
              <li>RAG très avancé (fine-tuning, hybrid search)</li>
              <li>Intégrations complexes (3-5 outils)</li>
              <li>Compliance (RGPD, sécurité renforcée)</li>
              <li>Monitoring avancé + SLA 99.9%</li>
              <li>Support prioritaire (1 mois)</li>
              <li>Calls de suivi hebdomadaires</li>
            </ul>
          </div>
          <div class="offer-use-cases">
            <p>
              <strong>Exemples :</strong> Architecture multi-agents RGPD/HIPAA, orchestration
              complexe
            </p>
          </div>
          <div class="offer-cta">
            <a href="#" class="btn btn-primary">🗓️ Réserver un appel</a>
            <a href="#" class="btn btn-secondary">✉️ Être recontacté</a>
          </div>
        </div>
      </div>

      <div class="offer-unsure">
        <p>Pas sûr de votre choix ?</p>
        <p>
          Commencez par le <strong>POC</strong> pour valider la faisabilité technique.
          <br />Ou réservez un appel découverte (30 min) pour analyser votre cas
          ensemble.
        </p>
      </div>
      <a href="/fr/contact" class="btn btn-primary"
        >Réserver un appel découverte</a
      >
    </div>
  </section>
</BaseLayout>

<!-- Services complémentaires -->
<section class="complementary-services section-spacing">
  <div class="container">
    <h2 class="section-title">Autres services</h2>

    <div class="services-grid">
      <div class="service-card">
        <div class="service-icon">◆</div>
        <h3>Audit d'agent IA</h3>
        <div class="service-price">2 500 €</div>
        <p class="service-vat">TVA non applicable, art. 293 B du CGI</p>
        <p>Vous avez déjà un agent IA qui ne fonctionne pas comme prévu ?</p>
        <div class="service-includes">
          <h4>Ce qui est inclus :</h4>
          <ul>
            <li>
              Audit technique complet (architecture, RAG, performance, sécurité)
            </li>
            <li>Audit métier (analyse des logs, taux de succès)</li>
            <li>Rapport détaillé (10-15 pages)</li>
            <li>Recommandations priorisées</li>
            <li>Call de restitution (1h)</li>
          </ul>
        </div>
        <p class="service-duration">⏱️ Durée : 1 semaine</p>
        <a href="#" class="btn btn-secondary">En savoir plus</a>
      </div>

      <div class="service-card">
        <div class="service-icon">●</div>
        <h3>Maintenance</h3>
        <div class="maintenance-options">
          <div class="maintenance-option">
            <h4>Light — 1 200 €/mois</h4>
            <ul>
              <li>Support réactif</li>
              <li>Correction bugs</li>
              <li>Mises à jour contenu</li>
              <li>1 call/mois</li>
            </ul>
          </div>
          <div class="maintenance-option">
            <h4>Pro — 2 000 €/mois</h4>
            <ul>
              <li>Tout Light +</li>
              <li>Optimisations</li>
              <li>Nouvelles features (1 jour/mois)</li>
              <li>2 calls/mois</li>
              <li>Rapport mensuel</li>
            </ul>
          </div>
        </div>
        <a href="#" class="btn btn-secondary">En savoir plus</a>
      </div>
    </div>
  </div>
</section>

<!-- Bloc Preuve (Portfolio) -->
<section class="proof-section section-spacing">
  <div class="container">
    <h2 class="section-title">Démo : Agent de support API</h2>

    <div class="proof-content">
      <div class="proof-image">
        <img src="/portfolio/01-home-welcome.png" alt="Agent de support API" />
      </div>
      <div class="proof-text">
        <p>
          Agent conversationnel pour support technique API. Répond aux questions
          sur l'authentification, les endpoints, les codes erreur. Disponible
          24/7, multilingue (FR/EN), avec citations des sources.
        </p>

        <h4>Ce que l'agent peut faire :</h4>
        <ul class="tech-stack">
          <li>
            <strong>Support technique :</strong> Répond aux questions API en temps
            réel
          </li>
          <li>
            <strong>Recherche intelligente :</strong> Trouve l'info dans 200+ pages
            de documentation
          </li>
          <li>
            <strong>Multilingue :</strong> Détection automatique français/anglais
          </li>
          <li>
            <strong>Citations sources :</strong> Toujours vérifiable et traçable
          </li>
        </ul>

        <h4>Bénéfices pour vos utilisateurs :</h4>
        <ul class="tech-stack">
          <li>
            <strong>Réponse instantanée :</strong> Quelques secondes vs plusieurs
            heures
          </li>
          <li>
            <strong>Disponibilité 24/7 :</strong> Plus besoin d'attendre les heures
            ouvrées
          </li>
          <li>
            <strong>Précision :</strong> Réponses contextuelles avec exemples de
            code
          </li>
          <li>
            <strong>Autonomie :</strong> Vos développeurs progressent sans bloquer
            votre support
          </li>
        </ul>

        <a href="/fr/portfolio" class="btn btn-primary">Tester la démo →</a>
      </div>
    </div>

    <!-- Portfolio supplémentaire -->
    <div class="portfolio-intro">
      <h3>Deux approches complémentaires</h3>
      <p>Je maîtrise différentes architectures d'agents IA selon vos besoins</p>
    </div>

    <div class="portfolio-grid-featured">
      <!-- Agent 1 : RAG -->
      <div class="portfolio-featured-card">
        <div class="featured-badge featured-badge-rag">Agent RAG</div>
        <PortfolioCard
          title="Neolens Support Agent"
          client="Support technique API"
          description="Agent RAG pour support documentation API. Recherche intelligente dans 200+ pages, citations sources, multilingue FR/EN."
          tags={["Python", "FastAPI", "LangGraph", "FAISS", "Next.js 14"]}
          image="/portfolio/01-home-welcome.png"
          link="/fr/portfolio"
          category={["ai-agent"]}
        />
        <div class="use-case-label">
          <strong>Cas d'usage :</strong> Documentation technique, FAQ, troubleshooting
        </div>
        <div class="demo-status">
          <span class="status-badge">📹 Démo vidéo disponible</span>
          <p>Voir les vidéos et la documentation technique complète</p>
        </div>
      </div>

      <!-- Agent 2 : Conversationnel -->
      <div class="portfolio-featured-card">
        <div class="featured-badge featured-badge-conversational">
          Agent Conversationnel
        </div>
        <PortfolioCard
          title="Visionary AI Agent"
          client="Assistant API conversationnel"
          description="Assistant qui accélère l'adoption de votre API. Quick-start prompts, exemples de code interactifs, copie en un clic. Vos développeurs autonomes en 10 minutes."
          tags={["React", "TypeScript", "Gemini AI", "Netlify Functions"]}
          image="/portfolio/visionary-ai-agent.png"
          link="https://visionary-ai-agent.netlify.app/"
          category={["ai-agent"]}
        />
        <div class="use-case-label">
          <strong>Cas d'usage :</strong> Onboarding développeur, adoption API, support
          intégration
        </div>
      </div>
    </div>

    <!-- Section comparative -->
    <div class="agent-comparison">
      <h3>Quelle approche pour votre projet ?</h3>

      <div class="comparison-cards">
        <div class="comparison-card">
          <h4>Agent RAG</h4>
          <p><strong>Idéal pour :</strong></p>
          <ul>
            <li>Documentation technique volumineuse</li>
            <li>Base de connaissances statique</li>
            <li>Besoin de citations sources</li>
            <li>Contenu peu changeant</li>
          </ul>
          <p class="example">Ex : Support technique, FAQ, compliance</p>
        </div>

        <div class="comparison-card">
          <h4>Agent Conversationnel</h4>
          <p><strong>Idéal pour :</strong></p>
          <ul>
            <li>Onboarding développeur</li>
            <li>Adoption produit/API</li>
            <li>Intégration complexe</li>
            <li>Exemples de code dynamiques</li>
          </ul>
          <p class="example">Ex : Assistant API, guide intégration</p>
        </div>
      </div>

      <p class="comparison-note">
        <strong>Pas sûr ?</strong> Je vous aide à choisir l'approche adaptée lors
        de l'appel découverte.
      </p>
    </div>

    <!-- Autres projets documentation -->
    <div class="portfolio-grid">
      <PortfolioCard
        title="Visionary API Documentation"
        client="Documentation technique"
        description="Documentation API complète pour l'agent Visionary, avec guides interactifs et onboarding développeur."
        tags={["API Documentation", "Docusaurus", "Interactive Guides"]}
        image="/portfolio/visionary-docs.png"
        link="https://visionary-api-docs.netlify.app/"
        category={["api-docs"]}
      />
      <PortfolioCard
        title="Neolens – Documentation API"
        client="IA fictive d'imagerie médicale"
        description="Documentation API complète pour l'analyse intelligente d'images médicales, avec guides interactifs et onboarding développeur."
        tags={["API Documentation", "Docusaurus", "Interactive Guides"]}
        image="/portfolio/neolens-api-docs.png"
        link="https://neolens-api-docs.netlify.app/"
        category={["api-docs"]}
      />
      <PortfolioCard
        title="Documentation API Cognitia sur GitBook"
        client="Plateforme IA Cognitia fictive"
        description="Plateforme de documentation API professionnelle construite sur GitBook avec recherche avancée, collaboration équipe et expérience développeur interactive"
        tags={["GitBook", "Team Collaboration", "Enterprise Docs"]}
        image="/portfolio/cognitia-api-docs.png"
        link="https://cognitia-api-documentation.gitbook.io/bgallais"
        category={["api-docs"]}
      />
    </div>
  </div>
</section>

<!-- CTA Final -->
<section class="cta-final section-spacing">
  <div class="container">
    <div class="cta-content">
      <h2>Discutons de votre projet</h2>
      <p>
        30 minutes pour analyser la faisabilité technique. Je vous dis si un
        agent IA est adapté à votre cas, et comment le construire.
      </p>
      <p class="cta-note">Pas de vente. Juste une discussion technique.</p>
      <a href="#" class="btn btn-primary btn-large">Prendre rendez-vous</a>
    </div>
  </div>
</section>

<BackToTop />

<style>
  /* ===============================
     HERO SECTION
     =============================== */
  .hero {
    padding: 8rem 0 6rem;
    background: linear-gradient(
      135deg,
      rgb(var(--color-background)) 0%,
      rgba(var(--color-primary), 0.05) 100%
    );
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: "";
    position: absolute;
    inset: 0;
    background: radial-gradient(
        circle at 20% 80%,
        rgba(var(--color-primary), 0.08) 0%,
        transparent 50%
      ),
      radial-gradient(
        circle at 80% 20%,
        rgba(var(--color-accent), 0.05) 0%,
        transparent 50%
      );
    pointer-events: none;
  }

  .hero .container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 5rem;
    align-items: center;
    position: relative;
    z-index: 1;
  }

  .hero-title {
    font-family: var(--font-hero);
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 700;
    line-height: 1.15;
    margin-bottom: 2rem;
    color: rgb(var(--color-text));
  }

  .hero-subtitle {
    font-size: 1.25rem;
    line-height: 1.7;
    color: rgb(var(--color-text-muted));
    margin-bottom: 2rem;
  }

  .hero-badge {
    display: inline-flex;
    padding: 0.75rem 1.5rem;
    background: rgba(var(--color-accent), 0.15);
    border: 1px solid rgba(var(--color-accent), 0.3);
    border-radius: var(--radius-lg);
    color: rgb(var(--color-accent));
    font-size: 0.875rem;
    font-weight: 500;
    margin-bottom: 2rem;
  }

  .hero-cta {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  /* Chat Preview */
  .chat-preview {
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow-xl);
    background: rgb(var(--color-surface));
    border: 1px solid rgba(var(--color-border), 0.3);
  }

  .chat-header {
    display: flex;
    align-items: center;
    padding: 1.25rem 1.5rem;
    gap: 1rem;
    background: rgba(var(--color-surface-variant), 0.3);
    border-bottom: 1px solid rgba(var(--color-border), 0.2);
  }

  .agent-avatar {
    font-size: 2rem;
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    background: linear-gradient(
      135deg,
      rgb(var(--color-primary)),
      rgb(var(--color-primary-hover))
    );
  }

  .agent-info {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .agent-name {
    font-weight: 600;
    font-size: 1rem;
    color: rgb(var(--color-text));
  }

  .agent-status {
    font-size: 0.875rem;
    color: rgb(var(--color-text-muted));
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .agent-status::before {
    content: "●";
    color: rgb(var(--color-success));
    font-size: 0.75rem;
  }

  .chat-messages {
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    min-height: 320px;
    background: rgba(var(--color-surface-variant), 0.1);
  }

  .message {
    display: flex;
    gap: 0.75rem;
    max-width: 90%;
    animation: slideIn 0.3s ease;
  }

  .user-message {
    align-self: flex-end;
    flex-direction: row-reverse;
  }

  .agent-message {
    align-self: flex-start;
  }

  .message-avatar {
    font-size: 1.5rem;
    flex-shrink: 0;
  }

  .message-content {
    padding: 1rem 1.25rem;
    border-radius: var(--radius-lg);
    font-size: 0.95rem;
    line-height: 1.6;
    flex: 1;
    text-align: left;
  }

  .user-message .message-content {
    background: rgb(var(--color-primary));
    color: rgb(var(--color-text-button));
    border-bottom-right-radius: 0.25rem;
  }

  .agent-message .message-content {
    background: rgba(var(--color-surface-variant), 0.8);
    color: rgb(var(--color-text));
    border-bottom-left-radius: 0.25rem;
    border: 1px solid rgba(var(--color-border), 0.7);
  }

  .inline-code {
    background: rgba(0, 0, 0, 0.2);
    padding: 0.25rem 0.5rem;
    border-radius: var(--radius-sm);
    font-family: var(--font-mono);
    font-size: 0.875rem;
    display: inline-block;
    text-align: left;
  }

  /* ===============================
   TECH BADGES (Hero)
   =============================== */
  .tech-badges {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
    margin-bottom: 2rem;
  }

  .tech-badges .badge {
    padding: 0.5rem 1rem;
    background: rgba(var(--color-surface-variant), 0.5);
    border: 1px solid rgba(var(--color-border), 0.5);
    border-radius: var(--radius-md);
    color: rgb(var(--color-text));
    font-size: 0.875rem;
    font-weight: 500;
  }

  /* ===============================
   CONTEXT SECTION
   =============================== */
  .context-section {
    background: rgb(var(--color-surface));
  }

  .context-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .context-card {
    background: rgba(var(--color-surface-variant), 0.3);
    border: 1px solid rgba(var(--color-border), 0.3);
    border-radius: var(--radius-lg);
    padding: 2.5rem;
  }

  .context-card--solution {
    background: rgba(var(--color-primary), 0.03);
    border: 1px solid rgba(var(--color-primary), 0.2);
  }

  .context-card h3 {
    font-family: var(--font-heading);
    font-size: 1.5rem;
    color: rgb(var(--color-text));
    margin-bottom: 1.5rem;
  }

  .context-card ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .context-card ul li {
    padding-left: 1.75rem;
    margin-bottom: 1rem;
    position: relative;
    color: rgb(var(--color-text-muted));
    line-height: 1.6;
  }

  .context-card ul li::before {
    content: "•";
    position: absolute;
    left: 0;
    color: rgb(var(--color-primary));
    font-size: 1.5rem;
    line-height: 1;
  }

  .context-card--solution ul li {
    padding-left: 2rem;
  }

  .context-card--solution ul li::before {
    content: "→";
    font-size: 1rem;
    line-height: 1.6;
  }

  .context-card strong {
    color: rgb(var(--color-text));
    font-weight: 600;
  }

  .context-result {
    text-align: center;
    padding: 2rem 2.5rem;
    background: rgba(var(--color-primary), 0.05);
    border: 2px solid rgba(var(--color-primary), 0.3);
    border-radius: var(--radius-lg);
    max-width: 900px;
    margin: 0 auto;
  }

  .context-result p {
    font-size: 1.125rem;
    color: rgb(var(--color-text));
    margin: 0;
    line-height: 1.7;
  }

  .context-result strong {
    color: rgb(var(--color-primary));
  }

  .cta-services {
    text-align: center;
    margin-top: 2.5rem;
    padding: 2rem;
    background: rgba(var(--color-accent), 0.03);
    border-radius: var(--radius-lg);
  }

  .cta-services p {
    font-size: 1.125rem;
    color: rgb(var(--color-text));
    margin-bottom: 1.5rem;
    font-weight: 500;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .context-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 768px) {
    .context-card {
      padding: 1.5rem;
    }

    .context-result {
      padding: 1.5rem;
    }
  }

  /* ===============================
   USE CASES & METRICS
   =============================== */
  .use-cases {
    margin-top: 4rem;
    padding: 2.5rem;
    background: rgba(var(--color-surface-variant), 0.3);
    border: 1px solid rgba(var(--color-border), 0.3);
    border-radius: var(--radius-lg);
  }

  .use-cases h3 {
    font-family: var(--font-heading);
    font-size: 1.5rem;
    color: rgb(var(--color-text));
    margin-bottom: 1.5rem;
    text-align: center;
  }

  .use-cases-list {
    list-style: none;
    padding: 0;
    max-width: 700px;
    margin: 0 auto 2rem;
  }

  .use-cases-list li {
    padding-left: 1.5rem;
    margin-bottom: 0.75rem;
    position: relative;
    color: rgb(var(--color-text-muted));
    font-size: 0.95rem;
  }

  .use-cases-list li::before {
    content: "→";
    position: absolute;
    left: 0;
    color: rgb(var(--color-primary));
    font-weight: bold;
  }

  .metrics-text {
    text-align: center;
    color: rgb(var(--color-text));
    font-size: 1rem;
    margin: 0;
  }

  /* ===============================
   APPROACH SECTION
   =============================== */
  .approach-section {
    background: rgb(var(--color-surface));
  }

  .approach-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
  }

  .approach-card {
    background: rgba(var(--color-surface-variant), 0.3);
    border: 1px solid rgba(var(--color-border), 0.3);
    border-radius: var(--radius-lg);
    padding: 2rem;
  }

  .approach-card h4 {
    font-family: var(--font-heading);
    font-size: 1.125rem;
    color: rgb(var(--color-text));
    margin-bottom: 0.75rem;
  }

  .approach-card p {
    color: rgb(var(--color-text-muted));
    font-size: 0.95rem;
    margin: 0;
  }

  /* ===============================
   OFFERS - SIMPLIFIED
   =============================== */
  .offer-tagline {
    font-size: 0.95rem;
    color: rgb(var(--color-text-muted));
    margin-bottom: 1rem;
    font-weight: 400;
  }

  .offer-use-cases {
    margin-top: 1.5rem;
    padding: 1rem;
    background: rgba(var(--color-accent), 0.05);
    border-radius: var(--radius-md);
    font-size: 0.9rem;
  }

  .offer-use-cases p {
    margin: 0;
    color: rgb(var(--color-text-muted));
  }

  .offer-use-cases strong {
    color: rgb(var(--color-text));
  }

  /* ===============================
   RESPONSIVE
   =============================== */
  @media (max-width: 1024px) {
    .approach-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 768px) {
    .tech-badges {
      justify-content: center;
    }

    .use-cases {
      padding: 1.5rem;
    }

    .approach-card {
      padding: 1.5rem;
    }
  }

  /* ===============================
   SECTIONS COMMUNES
   =============================== */
  .section-title {
    font-family: var(--font-heading);
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 700;
    text-align: center;
    margin-bottom: 3rem;
    color: rgb(var(--color-text));
  }

  .section-intro {
    text-align: center;
    font-size: 1.125rem;
    color: rgb(var(--color-text-muted));
    max-width: 800px;
    margin: 0 auto 3rem;
  }

  /* ===============================
     BLOC SOLUTION
     =============================== */
  .solution-section {
    background: rgba(var(--color-surface-variant), 0.5);
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .feature-card {
    background: rgb(var(--color-surface));
    border: 1px solid rgba(var(--color-border), 0.3);
    border-radius: var(--radius-lg);
    padding: 2rem;
    text-align: center;
    transition:
      transform 0.2s ease,
      box-shadow 0.2s ease;
  }

  .feature-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-xl);
  }

  .feature-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
    color: rgb(var(--color-primary));
    font-weight: 300;
    line-height: 1;
  }

  .feature-card h3 {
    font-family: var(--font-heading);
    font-size: 1.25rem;
    color: rgb(var(--color-text));
    margin-bottom: 1rem;
  }

  .feature-card p {
    color: rgb(var(--color-text-muted));
    font-size: 0.95rem;
  }

  .solution-result {
    text-align: center;
    padding: 2rem;
    background: rgba(var(--color-primary), 0.05);
    border: 2px solid rgba(var(--color-primary), 0.3);
    border-radius: var(--radius-lg);
  }

  .solution-result p {
    font-size: 1.125rem;
    color: rgb(var(--color-text));
    margin: 0;
  }

  .solution-result strong {
    color: rgb(var(--color-primary));
  }

  /* ===============================
     BLOC OFFRES
     =============================== */
  .offers-section {
    background: rgba(var(--color-surface-variant), 0.5);
  }

  .offers-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    margin-bottom: 3rem;
    align-items: start;
  }

  .offer-card {
    background: rgb(var(--color-surface));
    border: 1px solid rgba(var(--color-border), 0.3);
    border-radius: var(--radius-lg);
    padding: 2rem;
    position: relative;
    transition:
      transform 0.2s ease,
      box-shadow 0.2s ease;
    display: flex;
    flex-direction: column;
    height: 100%;
  }

  .offer-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-xl);
  }

  .offer-recommended {
    border: 2px solid rgb(var(--color-primary));
    box-shadow: 0 0 0 4px rgba(var(--color-primary), 0.1);
  }

  .offer-badge {
    position: absolute;
    top: -12px;
    right: 20px;
    background: linear-gradient(
      135deg,
      rgb(var(--color-primary)),
      rgb(var(--color-primary-hover))
    );
    color: rgb(var(--color-text-button));
    padding: 0.5rem 1rem;
    border-radius: var(--radius-lg);
    font-size: 0.875rem;
    font-weight: 600;
    box-shadow: var(--shadow-md);
  }

  .offer-header h3 {
    font-family: var(--font-heading);
    font-size: 1.75rem;
    color: rgb(var(--color-text));
    margin-bottom: 1rem;
  }

  .offer-price {
    font-size: 2.5rem;
    font-weight: 700;
    color: rgb(var(--color-primary));
    margin-bottom: 0.5rem;
  }

  .offer-vat {
    font-size: 0.75rem;
    color: rgb(var(--color-text-muted));
    font-style: italic;
    margin-bottom: 1.5rem;
  }

  .offer-for {
    background: rgba(var(--color-accent), 0.1);
    padding: 1rem;
    border-radius: var(--radius-md);
    margin-bottom: 1rem;
    font-size: 0.95rem;
    color: rgb(var(--color-text-muted));
  }

  .offer-for strong {
    color: rgb(var(--color-text));
  }

  .offer-subtitle {
    font-size: 1rem;
    color: rgb(var(--color-text));
    margin-bottom: 1rem;
    font-weight: 500;
  }

  .offer-duration {
    font-size: 0.9rem;
    color: rgb(var(--color-text-muted));
    margin-bottom: 1.5rem;
  }

  .offer-includes {
    flex: 1;
  }

  .offer-includes h4 {
    font-size: 1rem;
    color: rgb(var(--color-text));
    margin-bottom: 1rem;
  }

  .offer-includes ul {
    list-style: none;
    padding: 0;
    margin-bottom: 1.5rem;
  }

  .offer-includes ul li {
    padding-left: 1.5rem;
    margin-bottom: 0.75rem;
    position: relative;
    color: rgb(var(--color-text-muted));
    font-size: 0.95rem;
  }

  .offer-includes ul li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: rgb(var(--color-success));
    font-weight: bold;
  }

  .offer-cta {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    margin-top: auto;
    margin-bottom: 1.5rem;
  }

  .offer-cta .btn {
    width: 100%;
  }

  .offer-details {
    border-top: 1px solid rgba(var(--color-border), 0.3);
    padding-top: 1rem;
    margin-top: auto;
  }

  .offer-details summary {
    cursor: pointer;
    color: rgb(var(--color-primary));
    font-weight: 500;
    user-select: none;
    transition: opacity 0.2s ease;
  }

  .offer-details summary:hover {
    opacity: 0.8;
  }

  .offer-full-details {
    margin-top: 1rem;
    padding-top: 1rem;
    border-top: 1px solid rgba(var(--color-border), 0.2);
  }

  .offer-full-details p {
    margin-bottom: 0.75rem;
    color: rgb(var(--color-text));
  }

  .offer-full-details ul {
    list-style: none;
    padding: 0;
  }

  .offer-full-details ul li {
    padding-left: 1.5rem;
    margin-bottom: 0.5rem;
    position: relative;
    color: rgb(var(--color-text-muted));
    font-size: 0.9rem;
  }

  .offer-full-details ul li::before {
    content: "→";
    position: absolute;
    left: 0;
    color: rgb(var(--color-accent));
  }

  .offer-unsure {
    text-align: center;
    padding: 2rem;
    background: rgba(var(--color-primary), 0.05);
    border: 1px solid rgba(var(--color-primary), 0.2);
    border-radius: var(--radius-lg);
  }

  .offer-unsure p:first-child {
    font-size: 1.25rem;
    color: rgb(var(--color-text));
    font-weight: 600;
    margin-bottom: 0.75rem;
  }

  .offer-unsure p:last-of-type {
    color: rgb(var(--color-text-muted));
    margin-bottom: 1.5rem;
  }

  /* ===============================
   POC CARD - SPECIAL STYLING
   =============================== */

  /* Grid avec POC (4 colonnes) */
  .offers-grid--with-poc {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 1.5rem;
  }

  /* Réduire padding des cartes en mode 4 colonnes pour éviter troncature */
  .offers-grid--with-poc .offer-card {
    padding: 1.5rem;
  }

  .offers-grid--with-poc .offer-includes ul {
    font-size: 0.9rem;
  }

  /* Adapter les boutons en mode 4 colonnes */
  .offers-grid--with-poc .offer-cta .btn {
    font-size: 0.85rem;
    padding: 0.65rem 0.75rem;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  @media (max-width: 1400px) {
    .offers-grid--with-poc {
      grid-template-columns: repeat(2, 1fr);
      gap: 2rem;
    }

    /* Rétablir padding normal en mode 2 colonnes */
    .offers-grid--with-poc .offer-card {
      padding: 2rem;
    }

    .offers-grid--with-poc .offer-includes ul {
      font-size: 1rem;
    }

    /* Rétablir taille normale des boutons */
    .offers-grid--with-poc .offer-cta .btn {
      font-size: 1rem;
      padding: 0.875rem 1.5rem;
      white-space: normal;
      overflow: visible;
      text-overflow: clip;
    }
  }

  /* POC Card - Design distinct */
  .offer-card--poc {
    background: rgba(var(--color-accent), 0.03);
    border: 2px dashed rgba(var(--color-accent), 0.4);
  }

  .offer-card--poc:hover {
    border-color: rgba(var(--color-accent), 0.6);
    background: rgba(var(--color-accent), 0.05);
  }

  /* POC Badge */
  .offer-badge--poc {
    background: linear-gradient(
      135deg,
      rgb(var(--color-accent)),
      rgba(var(--color-accent), 0.8)
    );
  }

  /* Upgrade highlight */
  .offer-upgrade {
    margin-top: 1.5rem;
    padding: 1rem;
    background: rgba(var(--color-success), 0.08);
    border: 1px solid rgba(var(--color-success), 0.3);
    border-radius: var(--radius-md);
  }

  .offer-upgrade-text {
    margin: 0;
    font-size: 0.9rem;
    color: rgb(var(--color-text));
    text-align: center;
  }

  .offer-upgrade-text strong {
    color: rgb(var(--color-success));
  }

  /* Prix après POC */
  .offer-after-poc {
    font-size: 0.95rem;
    color: rgb(var(--color-success));
    margin-top: 0.5rem;
    font-weight: 600;
  }

  /* ===============================
   RESPONSIVE - POC
   =============================== */

  @media (max-width: 1400px) {
    .offers-grid--with-poc {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .offers-grid--with-poc {
      grid-template-columns: 1fr;
    }

    .offer-card--poc {
      order: -1; /* POC en premier sur mobile */
    }
  }

  /* ===============================
     SERVICES COMPLÉMENTAIRES
     =============================== */
  .complementary-services {
    background: rgb(var(--color-surface));
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
  }

  .service-card {
    background: rgba(var(--color-surface-variant), 0.3);
    border: 1px solid rgba(var(--color-border), 0.3);
    border-radius: var(--radius-lg);
    padding: 2rem;
  }

  .service-icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    color: rgb(var(--color-primary));
  }

  .service-card h3 {
    font-family: var(--font-heading);
    font-size: 1.5rem;
    color: rgb(var(--color-text));
    margin-bottom: 1rem;
  }

  .service-price {
    font-size: 2rem;
    font-weight: 700;
    color: rgb(var(--color-primary));
    margin-bottom: 0.5rem;
  }

  .service-vat {
    font-size: 0.75rem;
    color: rgb(var(--color-text-muted));
    font-style: italic;
    margin-bottom: 1rem;
  }

  .service-includes h4 {
    font-size: 1rem;
    color: rgb(var(--color-text));
    margin-bottom: 1rem;
    margin-top: 1.5rem;
  }

  .service-includes ul {
    list-style: none;
    padding: 0;
    margin-bottom: 1.5rem;
  }

  .service-includes ul li {
    padding-left: 1.5rem;
    margin-bottom: 0.75rem;
    position: relative;
    color: rgb(var(--color-text-muted));
    font-size: 0.95rem;
  }

  .service-includes ul li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: rgb(var(--color-success));
    font-weight: bold;
  }

  .service-duration {
    font-size: 0.9rem;
    color: rgb(var(--color-text-muted));
    margin-bottom: 1.5rem;
  }

  .maintenance-options {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    margin-top: 1.5rem;
    margin-bottom: 1.5rem;
  }

  .maintenance-option {
    background: rgba(var(--color-surface-variant), 0.5);
    padding: 1.5rem;
    border-radius: var(--radius-md);
    border: 1px solid rgba(var(--color-border), 0.3);
  }

  .maintenance-option h4 {
    color: rgb(var(--color-text));
    margin-bottom: 1rem;
    font-size: 1.125rem;
  }

  .maintenance-option ul {
    list-style: none;
    padding: 0;
  }

  .maintenance-option ul li {
    padding-left: 1.5rem;
    margin-bottom: 0.5rem;
    position: relative;
    color: rgb(var(--color-text-muted));
    font-size: 0.9rem;
  }

  .maintenance-option ul li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: rgb(var(--color-success));
  }

  /* ===============================
     BLOC PREUVE
     =============================== */
  .proof-section {
    background: rgba(var(--color-surface-variant), 0.5);
  }

  .proof-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: center;
    margin-bottom: 4rem;
  }

  .proof-image img {
    width: 100%;
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-xl);
    border: 1px solid rgba(var(--color-border), 0.3);
  }

  .proof-text p {
    color: rgb(var(--color-text-muted));
    margin-bottom: 1.5rem;
  }

  .proof-text h4 {
    color: rgb(var(--color-text));
    margin-bottom: 1rem;
    margin-top: 2rem;
  }

  .tech-stack {
    list-style: none;
    padding: 0;
    margin-bottom: 2rem;
  }

  .tech-stack li {
    padding-left: 1.5rem;
    margin-bottom: 0.75rem;
    position: relative;
    color: rgb(var(--color-text-muted));
  }

  .tech-stack li::before {
    content: "▸";
    position: absolute;
    left: 0;
    color: rgb(var(--color-primary));
  }

  .tech-stack strong {
    color: rgb(var(--color-text));
  }

  /* ===============================
     PORTFOLIO - INTRO
     =============================== */
  .portfolio-intro {
    text-align: center;
    margin-bottom: 3rem;
  }

  .portfolio-intro h3 {
    font-family: var(--font-heading);
    font-size: 2rem;
    color: rgb(var(--color-text));
    margin-bottom: 1rem;
  }

  .portfolio-intro p {
    font-size: 1.125rem;
    color: rgb(var(--color-text-muted));
    max-width: 600px;
    margin: 0 auto;
  }

  /* ===============================
     PORTFOLIO - FEATURED GRID
     =============================== */
  .portfolio-grid-featured {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 3rem;
    margin-bottom: 4rem;
  }

  .portfolio-featured-card {
    position: relative;
    background: rgba(var(--color-surface-variant), 0.3);
    border: 2px solid rgba(var(--color-border), 0.3);
    border-radius: var(--radius-lg);
    padding: 2rem;
    transition: all 0.3s ease;
  }

  .portfolio-featured-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-xl);
    border-color: rgba(var(--color-primary), 0.5);
  }

  .featured-badge {
    position: absolute;
    top: -12px;
    left: 20px;
    padding: 0.5rem 1rem;
    border-radius: var(--radius-md);
    font-size: 0.875rem;
    font-weight: 600;
    box-shadow: var(--shadow-md);
    z-index: 1;
  }

  .featured-badge-rag {
    background: linear-gradient(
      135deg,
      rgb(var(--color-primary)),
      rgb(var(--color-primary-hover))
    );
    color: rgb(var(--color-text-button));
  }

  .featured-badge-conversational {
    background: linear-gradient(
      135deg,
      rgb(var(--color-accent)),
      rgba(var(--color-accent), 0.8)
    );
    color: rgb(var(--color-text-button));
  }

  .use-case-label {
    margin-top: 1rem;
    padding: 1rem;
    background: rgba(var(--color-primary), 0.05);
    border-radius: var(--radius-md);
    font-size: 0.9rem;
    color: rgb(var(--color-text-muted));
    border-left: 3px solid rgb(var(--color-primary));
  }

  .use-case-label strong {
    color: rgb(var(--color-text));
  }

  .demo-status {
    margin-top: 1rem;
    padding: 1rem;
    background: rgba(var(--color-accent), 0.05);
    border-radius: var(--radius-md);
    text-align: center;
  }

  .status-badge {
    display: inline-block;
    padding: 0.375rem 0.75rem;
    background: rgba(var(--color-accent), 0.15);
    border: 1px solid rgba(var(--color-accent), 0.3);
    border-radius: var(--radius-sm);
    font-size: 0.875rem;
    font-weight: 600;
    color: rgb(var(--color-text));
    margin-bottom: 0.5rem;
  }

  .demo-status p {
    margin: 0.5rem 0 0 0;
    font-size: 0.875rem;
    color: rgb(var(--color-text-muted));
  }

  /* ===============================
     AGENT COMPARISON
     =============================== */
  .agent-comparison {
    margin: 4rem 0;
    padding: 3rem 2rem;
    background: rgba(var(--color-surface-variant), 0.2);
    border-radius: var(--radius-lg);
  }

  .agent-comparison h3 {
    font-family: var(--font-heading);
    font-size: 1.75rem;
    color: rgb(var(--color-text));
    text-align: center;
    margin-bottom: 2.5rem;
  }

  .comparison-cards {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
    margin-bottom: 2rem;
  }

  .comparison-card {
    background: rgb(var(--color-surface));
    border: 1px solid rgba(var(--color-border), 0.3);
    border-radius: var(--radius-lg);
    padding: 2rem;
  }

  .comparison-card h4 {
    font-family: var(--font-heading);
    font-size: 1.25rem;
    color: rgb(var(--color-primary));
    margin-bottom: 1rem;
  }

  .comparison-card p {
    margin-bottom: 0.75rem;
    color: rgb(var(--color-text));
  }

  .comparison-card ul {
    list-style: none;
    padding: 0;
    margin: 1rem 0;
  }

  .comparison-card ul li {
    padding-left: 1.5rem;
    margin-bottom: 0.5rem;
    position: relative;
    color: rgb(var(--color-text-muted));
    font-size: 0.9rem;
  }

  .comparison-card ul li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: rgb(var(--color-success));
    font-weight: bold;
  }

  .comparison-card .example {
    margin-top: 1rem;
    padding: 0.75rem;
    background: rgba(var(--color-accent), 0.05);
    border-radius: var(--radius-md);
    font-size: 0.875rem;
    color: rgb(var(--color-text-muted));
    font-style: italic;
  }

  .comparison-note {
    text-align: center;
    font-size: 1.125rem;
    color: rgb(var(--color-text));
    padding: 1.5rem;
    background: rgba(var(--color-primary), 0.05);
    border-radius: var(--radius-md);
    margin: 0;
  }

  .comparison-note strong {
    color: rgb(var(--color-primary));
  }

  /* ===============================
     PORTFOLIO GRID (autres projets)
     =============================== */
  .portfolio-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 2rem;
  }

  /* ===============================
     RESPONSIVE - PORTFOLIO
     =============================== */
  @media (max-width: 1024px) {
    .portfolio-grid-featured {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .comparison-cards {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }
  }

  @media (max-width: 768px) {
    .portfolio-featured-card {
      padding: 1.5rem;
    }

    .agent-comparison {
      padding: 2rem 1rem;
    }

    .comparison-card {
      padding: 1.5rem;
    }
  }

  /* ===============================
     CTA FINAL
     =============================== */
  .cta-final {
    background: linear-gradient(
      135deg,
      rgba(var(--color-primary), 0.08) 0%,
      rgba(var(--color-accent), 0.05) 100%
    );
    position: relative;
    overflow: hidden;
  }

  .cta-final::before {
    content: "";
    position: absolute;
    inset: 0;
    background: radial-gradient(
      circle at 30% 70%,
      rgba(var(--color-primary), 0.1) 0%,
      transparent 50%
    );
    pointer-events: none;
  }

  .cta-content {
    text-align: center;
    max-width: 700px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  .cta-content h2 {
    font-family: var(--font-heading);
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 700;
    margin-bottom: 1.5rem;
    color: rgb(var(--color-text));
  }

  .cta-content p {
    font-size: 1.125rem;
    color: rgb(var(--color-text-muted));
    margin-bottom: 1rem;
  }

  .cta-no-bs {
    color: rgb(var(--color-primary)) !important;
    font-size: 1rem !important;
    margin-bottom: 2rem !important;
  }

  .cta-note {
    color: rgb(var(--color-text)) !important;
    font-size: 1rem !important;
    margin-bottom: 2rem !important;
    font-weight: 500;
  }

  /* ===============================
     RESPONSIVE
     =============================== */
  @media (max-width: 1024px) {
    .hero .container,
    .proof-content {
      grid-template-columns: 1fr;
      gap: 3rem;
      text-align: center;
    }

    .hero-cta {
      justify-content: center;
    }

    .chat-preview {
      max-width: 600px;
      margin: 0 auto;
    }

    .offers-grid,
    .differentiation-points,
    .features-grid {
      grid-template-columns: 1fr;
    }

    .problem-grid,
    .services-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding: 5rem 0 4rem;
    }

    .section-spacing {
      padding: 3rem 0;
    }

    .comparison-table {
      font-size: 0.8rem;
    }

    .comparison-table th,
    .comparison-table td {
      padding: 0.75rem 0.5rem;
    }

    .offer-card {
      padding: 1.5rem;
    }

    .message {
      max-width: 95%;
    }

    .message-avatar {
      font-size: 1.25rem;
    }
  }

  @media (max-width: 600px) {
    .hero-title {
      font-size: 2rem;
    }

    .hero-subtitle {
      font-size: 1rem;
    }

    .section-title {
      font-size: 1.75rem;
    }

    .btn {
      width: 100%;
    }

    .hero-cta .btn,
    .offer-cta .btn {
      font-size: 0.9rem;
      padding: 0.75rem 1.25rem;
    }

    .chat-messages {
      min-height: 280px;
      padding: 1rem;
    }

    .comparison-table th,
    .comparison-table td {
      padding: 0.5rem 0.25rem;
      font-size: 0.75rem;
    }
  }

  /* ===============================
     SECTION MARCHÉ
     =============================== */
  .market-section {
    background: linear-gradient(
      135deg,
      rgba(var(--color-primary), 0.03) 0%,
      rgba(var(--color-accent), 0.02) 100%
    );
  }

  .market-stats {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .stat-card {
    background: rgb(var(--color-surface));
    border: 1px solid rgba(var(--color-border), 0.3);
    border-radius: var(--radius-lg);
    padding: 2rem;
    text-align: center;
    transition:
      transform 0.2s ease,
      box-shadow 0.2s ease;
  }

  .stat-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-xl);
  }

  .stat-number {
    font-size: 3.5rem;
    font-weight: 700;
    color: rgb(var(--color-primary));
    margin-bottom: 1rem;
    line-height: 1;
  }

  .stat-card p {
    color: rgb(var(--color-text));
    font-size: 1rem;
    line-height: 1.5;
    margin-bottom: 1rem;
  }

  .stat-source {
    display: block;
    font-size: 0.75rem;
    color: rgb(var(--color-text-muted));
    font-style: italic;
  }

  .market-insight {
    text-align: center;
    max-width: 900px;
    margin: 0 auto;
    padding: 2.5rem;
    background: rgba(var(--color-primary), 0.05);
    border: 2px solid rgba(var(--color-primary), 0.2);
    border-radius: var(--radius-lg);
  }

  .market-insight p {
    font-size: 1.125rem;
    color: rgb(var(--color-text));
    margin-bottom: 1rem;
    line-height: 1.7;
  }

  .market-insight p:last-child {
    margin-bottom: 0;
  }

  .market-source {
    font-size: 0.875rem !important;
    color: rgb(var(--color-text-muted)) !important;
    margin-top: 1rem !important;
  }

  @media (max-width: 1024px) {
    .market-stats {
      grid-template-columns: repeat(2, 1fr);
      gap: 1.5rem;
    }

    .stat-number {
      font-size: 3rem;
    }
  }

  @media (max-width: 600px) {
    .market-stats {
      grid-template-columns: 1fr;
      gap: 1rem;
    }

    .stat-card {
      padding: 1.5rem;
    }

    .stat-number {
      font-size: 2.5rem;
    }

    .market-insight {
      padding: 1.5rem;
    }

    .market-insight p {
      font-size: 1rem;
    }
  }
</style>
