---
// src/pages/fr/index.astro
import BaseLayout from "../../layouts/BaseLayout.astro";
import PortfolioCard from "../../components/PortfolioCard.astro";
import BackToTop from "../../components/BackToTop.astro";
---

<BaseLayout
  title="AI Agent Engineer | Systèmes conversationnels en production"
  description="Je construis des agents IA conversationnels avec LangGraph, FastAPI et React. Code propre, testé, documenté. Livré en 4-6 semaines."
  lang="fr"
>
  <!-- Hero Section -->
  <section class="hero">
    <div class="container">
      <div class="hero-content">
        <h1 class="hero-title">
          AI Agent Engineer Freelance<span class="gradient-text"
            >Systèmes conversationnels en production</span
          >
        </h1>
        <p class="hero-subtitle">
          Je construis des agents IA conversationnels avec LangGraph, FastAPI et
          React. Code propre, testé, documenté. Livré en 4-6 semaines.
        </p>
        <div class="tech-badges">
          <span class="badge">Python</span>
          <span class="badge">FastAPI</span>
          <span class="badge">LangGraph</span>
          <span class="badge">React</span>
          <span class="badge">FAISS</span>
          <span class="badge">PostgreSQL</span>
        </div>
        <div class="hero-cta">
          <a href="#offres" class="btn btn-primary">Voir les offres</a>
          <a
            href="https://visionary-ai-agent.netlify.app/"
            class="btn btn-secondary"
            target="_blank">Tester la démo</a
          >
        </div>
      </div>

      <div class="chat-preview">
        <div class="chat-header">
          <div class="agent-avatar">🤖</div>
          <div class="agent-info">
            <span class="agent-name">Assistant IA</span>
            <span class="agent-status">En ligne</span>
          </div>
        </div>
        <div class="chat-messages">
          <div class="message user-message">
            <div class="message-avatar">👤</div>
            <div class="message-content">
              Comment authentifier mes requêtes API ?
            </div>
          </div>
          <div class="message agent-message">
            <div class="message-avatar">🤖</div>
            <div class="message-content">
              Pour authentifier vos requêtes, utilisez un Bearer token dans le
              header Authorization.<br /><br />
              <code class="inline-code">Authorization: Bearer YOUR_TOKEN</code
              ><br /><br />
              Vous pouvez générer votre token dans les paramètres de votre compte.
              Le token a une durée de validité de 30 jours.<br /><br />
              Besoin d'aide pour générer votre token ?
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Problématique & Solutions -->
  <section class="context-section section-spacing">
    <div class="container">
      <h2 class="section-title">Pour qui ?</h2>

      <div class="context-grid">
        <div class="context-card">
          <h3>Vous avez ce problème</h3>
          <ul>
            <li>
              Votre équipe technique fait du support au lieu de développer
            </li>
            <li>Votre documentation est complète mais personne ne la lit</li>
            <li>Vous gérez 500+ tickets/mois dont 60% sont répétitifs</li>
            <li>
              Les chatbots no-code que vous avez testés sont trop limités pour
              vous et donnent des réponses génériques
            </li>
          </ul>
        </div>

        <div class="context-card context-card--solution">
          <h3>Ce que je construis</h3>
          <ul>
            <li>
              <strong>Assistant support technique</strong> : répond aux questions
              sur votre produit, votre API, votre documentation. Sans hallucination
            </li>
            <li>
              <strong>Agent de qualification commerciale</strong> : qualifie les
              leads, collecte les besoins, dirige vers le bon interlocuteur
            </li>
            <li>
              <strong>Assistant onboarding</strong> : guide les nouveaux utilisateurs
              ou collaborateurs, explique les fonctionnalités, réduit le time-to-value
            </li>
            <li>
              <strong>Assistant interne</strong> : RH, IT, compliance — répond aux
              questions récurrentes de vos équipes
            </li>
          </ul>
        </div>
      </div>

      <div class="context-result">
        <p>
          <strong>Résultat concret :</strong> 80% des questions niveau 1 automatisées.
          Votre équipe se concentre sur les cas complexes. Vos utilisateurs ont leurs
          réponses en quelquesberan secondes
        </p>
      </div>
    </div>
  </section>

  <!-- Architecture & Approche -->
  <section class="solution-section section-spacing">
    <div class="container">
      <h2 class="section-title">Architecture RAG complète</h2>

      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon">◆</div>
          <h3>Retrieval-Augmented Generation</h3>
          <p>
            Vector search, reranking, citation des sources. Répond avec
            précision à partir de vos données.
          </p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">◆</div>
          <h3>Intégrations métier</h3>
          <p>
            Connexion à vos APIs, CRM, documentation, Slack, bases de données.
            L'agent accède à vos propres données.
          </p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">◆</div>
          <h3>Production-ready</h3>
          <p>
            Tests automatisés (pytest, CI/CD), observabilité complète (logs,
            metrics, monitoring), documentation technique.
          </p>
        </div>
      </div>

      <div class="use-cases">
        <h3>Cas d'usage typiques</h3>
        <ul class="use-cases-list">
          <li>Support technique automatisé (FAQ, docs, troubleshooting)</li>
          <li>Qualification de leads commerciaux</li>
          <li>Onboarding utilisateurs</li>
          <li>Assistants internes (RH, IT, compliance)</li>
        </ul>
        <p class="metrics-text">
          <strong>Métriques moyennes :</strong> 80% des requêtes niveau 1 résolues
          • Temps de réponse &lt;2s
        </p>
      </div>
    </div>
  </section>

  <!-- Approche -->
  <section class="approach-section section-spacing">
    <div class="container">
      <h2 class="section-title">Approche</h2>

      <div class="approach-grid">
        <div class="approach-card">
          <h4>Contact direct</h4>
          <p>
            Pas d'intermédiaire. Suivi quotidien de l'avancement. Vous savez
            toujours où vous en êtes.
          </p>
        </div>

        <div class="approach-card">
          <h4>Tests automatisés complets</h4>
          <p>
            Tests unitaires, tests d'intégration, CI/CD. Pas optionnels. Vous
            recevez un système qui fonctionne.
          </p>
        </div>

        <div class="approach-card">
          <h4>Documentation complète</h4>
          <p>
            Documentation technique (architecture, APIs, déploiement) ET
            documentation utilisateur. Pas de boîte noire.
          </p>
        </div>

        <div class="approach-card">
          <h4>Code ownership</h4>
          <p>
            Vous recevez tout le code, commenté, versionné, déployable. Pas de
            dépendance. Pas de vendor lock-in.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Offres -->
  <section id="offres" class="offers-section section-spacing">
    <div class="container">
      <h2 class="section-title">Offres</h2>

      <div class="offers-grid offers-grid--with-poc">
        <!-- POC (Nouveau) -->
        <div class="offer-card offer-card--poc">
          <div class="offer-badge offer-badge--poc">🧪 Validation</div>
          <div class="offer-header">
            <h3>POC</h3>
            <p class="offer-tagline">Audit + Prototype fonctionnel</p>
            <div class="offer-price">5 000€</div>
            <p class="offer-vat">TVA non applicable, art. 293 B du CGI</p>
          </div>
          <div class="offer-duration">
            ⏱️ Durée approximative : 2-3 semaines
          </div>
          <div class="offer-includes">
            <h4>Livrables</h4>
            <ul>
              <li>Audit documentation complet</li>
              <li>Prototype agent RAG fonctionnel (15-25 pages)</li>
              <li>Tests 20 questions types</li>
              <li>Rapport détaillé (10-15 pages)</li>
              <li>Recommandations stratégiques</li>
              <li>Call restitution (1h30)</li>
              <li>Accès démo 14 jours</li>
            </ul>
          </div>
          <div class="offer-upgrade">
            <p class="offer-upgrade-text">
              <strong>🎁 Upgrade :</strong> 2 500€ déduits si passage au Starter
              ou Scale
            </p>
          </div>
          <div class="offer-use-cases">
            <p>
              <strong>Pour qui :</strong> Premier projet IA, besoin de validation
              technique avant investissement complet
            </p>
          </div>
          <div class="offer-cta">
            <a href="#" class="btn btn-primary">🗓️ Réserver un appel (30 min)</a
            >
            <a href="#" class="btn btn-secondary">✉️ Être recontacté sous 24h</a
            >
          </div>
        </div>

        <!-- Starter -->
        <div class="offer-card">
          <div class="offer-header">
            <h3>Starter</h3>
            <p class="offer-tagline">Agent conversationnel fonctionnel</p>
            <div class="offer-price">10 000€</div>
            <p class="offer-vat">TVA non applicable, art. 293 B du CGI</p>
            <p class="offer-after-poc">Ou 7 500€ après POC</p>
          </div>
          <div class="offer-duration">
            ⏱️ Durée approximative : 3-4 semaines
          </div>
          <div class="offer-includes">
            <h4>Livrables</h4>
            <ul>
              <li>Agent conversationnel RAG fonctionnel</li>
              <li>Backend Python + FastAPI</li>
              <li>Frontend React responsive</li>
              <li>Tests unitaires</li>
              <li>Documentation technique</li>
              <li>Déploiement + hébergement</li>
              <li>2 semaines de support</li>
            </ul>
          </div>
          <div class="offer-use-cases">
            <p>
              <strong>Exemples :</strong> FAQ automatisée, assistant docs, pré-qualification
              leads
            </p>
          </div>
          <div class="offer-cta">
            <a href="#" class="btn btn-primary">🗓️ Réserver un appel (30 min)</a
            >
            <a href="#" class="btn btn-secondary">✉️ Être recontacté sous 24h</a
            >
          </div>
        </div>

        <!-- Scale (Recommandé) -->
        <div class="offer-card offer-recommended">
          <div class="offer-badge">⭐ Recommandé</div>
          <div class="offer-header">
            <h3>Scale</h3>
            <p class="offer-tagline">Système RAG complet</p>
            <div class="offer-price">15 000€</div>
            <p class="offer-vat">TVA non applicable, art. 293 B du CGI</p>
            <p class="offer-after-poc">Ou 12 500€ après POC</p>
          </div>
          <div class="offer-duration">
            ⏱️ Durée approximative : 4-6 semaines
          </div>
          <div class="offer-includes">
            <h4>Livrables</h4>
            <ul>
              <li>Tout ce qui est dans Starter</li>
              <li>RAG avancé (multi-sources, reranking)</li>
              <li>1-2 intégrations (CRM, API, Slack...)</li>
              <li>Tests automatisés complets (CI/CD)</li>
              <li>Observabilité complète (dashboard)</li>
              <li>Documentation technique + utilisateur</li>
              <li>Formation équipe (2h)</li>
              <li>1 mois de support</li>
            </ul>
          </div>
          <div class="offer-use-cases">
            <p>
              <strong>Exemples :</strong> Support multilingue, onboarding sécurisé,
              assistant compliance
            </p>
          </div>
          <div class="offer-cta">
            <a href="#" class="btn btn-primary">🗓️ Réserver un appel (30 min)</a
            >
            <a href="#" class="btn btn-secondary">✉️ Être recontacté sous 24h</a
            >
          </div>
        </div>

        <!-- Enterprise -->
        <div class="offer-card">
          <div class="offer-header">
            <h3>Enterprise</h3>
            <p class="offer-tagline">Architecture multi-agents</p>
            <div class="offer-price">30 000€</div>
            <p class="offer-vat">TVA non applicable, art. 293 B du CGI</p>
          </div>
          <div class="offer-duration">
            ⏱️ Durée approximative : 6-8 semaines
          </div>
          <div class="offer-includes">
            <h4>Livrables</h4>
            <ul>
              <li>Tout ce qui est dans Scale</li>
              <li>Architecture multi-agents avec orchestration</li>
              <li>RAG très avancé (fine-tuning, hybrid search)</li>
              <li>Intégrations complexes (3-5 outils)</li>
              <li>Compliance (RGPD, sécurité renforcée)</li>
              <li>Monitoring avancé + SLA 99.9%</li>
              <li>Support prioritaire (1 mois)</li>
              <li>Calls de suivi hebdomadaires</li>
            </ul>
          </div>
          <div class="offer-use-cases">
            <p>
              <strong>Exemples :</strong> Architecture multi-agents RGPD/HIPAA, orchestration
              complexe
            </p>
          </div>
          <div class="offer-cta">
            <a href="#" class="btn btn-primary">🗓️ Réserver un appel (30 min)</a
            >
            <a href="#" class="btn btn-secondary">✉️ Être recontacté sous 24h</a
            >
          </div>
        </div>
      </div>

      <div class="offer-unsure">
        <p>Pas sûr de votre choix ?</p>
        <p>
          Commencez par le <strong>POC (5 000€)</strong> pour valider la faisabilité
          technique, ou réservez un appel découverte pour qu'on analyse votre cas
          ensemble.
        </p>
        <a href="#" class="btn btn-primary">Réserver un appel découverte</a>
      </div>
    </div>
  </section>

  <!-- Services complémentaires -->
  <section class="complementary-services section-spacing">
    <div class="container">
      <h2 class="section-title">Autres services</h2>

      <div class="services-grid">
        <div class="service-card">
          <div class="service-icon">◆</div>
          <h3>Audit d'agent IA</h3>
          <div class="service-price">2 500€</div>
          <p class="service-vat">TVA non applicable, art. 293 B du CGI</p>
          <p>Vous avez déjà un agent IA qui ne fonctionne pas comme prévu ?</p>
          <div class="service-includes">
            <h4>Ce qui est inclus :</h4>
            <ul>
              <li>
                Audit technique complet (architecture, RAG, performance,
                sécurité)
              </li>
              <li>Audit métier (analyse des logs, taux de succès)</li>
              <li>Rapport détaillé (10-15 pages)</li>
              <li>Recommandations priorisées</li>
              <li>Call de restitution (1h)</li>
            </ul>
          </div>
          <p class="service-duration">⏱️ Durée : 1 semaine</p>
          <a href="#" class="btn btn-secondary">En savoir plus</a>
        </div>

        <div class="service-card">
          <div class="service-icon">●</div>
          <h3>Maintenance</h3>
          <div class="maintenance-options">
            <div class="maintenance-option">
              <h4>Light — 1 200€/mois</h4>
              <ul>
                <li>Support réactif</li>
                <li>Correction bugs</li>
                <li>Mises à jour contenu</li>
                <li>1 call/mois</li>
              </ul>
            </div>
            <div class="maintenance-option">
              <h4>Pro — 2 000€/mois</h4>
              <ul>
                <li>Tout Light +</li>
                <li>Optimisations</li>
                <li>Nouvelles features (1 jour/mois)</li>
                <li>2 calls/mois</li>
                <li>Rapport mensuel</li>
              </ul>
            </div>
          </div>
          <a href="#" class="btn btn-secondary">En savoir plus</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Bloc Preuve (Portfolio) -->
  <section class="proof-section section-spacing">
    <div class="container">
      <h2 class="section-title">
        Mon propre agent RAG : la preuve que ça marche
      </h2>

      <div class="proof-content">
        <div class="proof-image">
          <img
            src="/portfolio/visionary-ai-agent.png"
            alt="Visionary AI Agent"
          />
        </div>
        <div class="proof-text">
          <p>
            J'ai développé un agent conversationnel avec RAG pour ma
            documentation technique. Architecture LangGraph, backend
            Python/FastAPI, frontend React. Fonctionnel, testé, déployé.
          </p>

          <h4>Stack technique :</h4>
          <ul class="tech-stack">
            <li><strong>Backend :</strong> Python, FastAPI, LangGraph</li>
            <li><strong>RAG :</strong> FAISS, embeddings OpenAI</li>
            <li><strong>Frontend :</strong> React, Tailwind CSS</li>
            <li>
              <strong>Tests :</strong> Pytest, tests unitaires + intégration
            </li>
            <li><strong>Déploiement :</strong> Render</li>
          </ul>

          <a href="/fr/portfolio" class="btn btn-primary"
            >Voir le projet complet →</a
          >
        </div>
      </div>

      <!-- Portfolio supplémentaire -->
      <div class="portfolio-grid">
        <PortfolioCard
          title="Visionary AI Agent"
          client="Assistant conversationnel IA"
          description="Agent conversationnel intelligent pour API d'analyse d'images. Démontre l'approche hybride conversationnelle avec knowledge base structurée."
          tags={["React", "TypeScript", "Gemini AI", "Netlify Functions"]}
          image="/portfolio/visionary-ai-agent.png"
          link="https://visionary-ai-agent.netlify.app/"
          category={["ai-agent"]}
        />
        <PortfolioCard
          title="Agent RAG (Coming Soon)"
          client="Projet en développement"
          description="Système RAG avancé avec multi-sources et reranking. Disponible la semaine prochaine."
          tags={["Python", "FastAPI", "LangGraph", "FAISS"]}
          image="/portfolio/coming-soon.png"
          link="#"
          category={["ai-agent"]}
        />
        <PortfolioCard
          title="Visionary API Documentation"
          client="Documentation technique"
          description="Documentation API complète pour l'agent Visionary, avec guides interactifs et onboarding développeur."
          tags={["API Documentation", "Docusaurus", "Interactive Guides"]}
          image="/portfolio/visionary-docs.png"
          link="https://visionary-api-docs.netlify.app/"
          category={["api-docs"]}
        />
        <PortfolioCard
          title="Neolens – Documentation API"
          client="IA fictive d'imagerie médicale"
          description="Documentation API complète pour l'analyse intelligente d'images médicales, avec guides interactifs et onboarding développeur."
          tags={["API Documentation", "Docusaurus", "Interactive Guides"]}
          image="/portfolio/neolens-api-docs.png"
          link="https://neolens-api-docs.netlify.app/"
          category={["api-docs"]}
        />
        <PortfolioCard
          title="Documentation API Cognitia sur GitBook"
          client="Plateforme IA Cognitia fictive"
          description="Plateforme de documentation API professionnelle construite sur GitBook avec recherche avancée, collaboration équipe et expérience développeur interactive"
          tags={["GitBook", "Team Collaboration", "Enterprise Docs"]}
          image="/portfolio/cognitia-api-docs.png"
          link="https://cognitia-api-documentation.gitbook.io/bgallais"
          category={["api-docs"]}
        />
      </div>
    </div>
  </section>

  <!-- CTA Final -->
  <section class="cta-final section-spacing">
    <div class="container">
      <div class="cta-content">
        <h2>Discutons de votre projet</h2>
        <p>
          30 minutes pour analyser la faisabilité technique. Je vous dis si un
          agent IA est adapté à votre cas, et comment le construire.
        </p>
        <p class="cta-note">Pas de vente. Juste une discussion technique.</p>
        <a href="#" class="btn btn-primary btn-large">Prendre rendez-vous</a>
      </div>
    </div>
  </section>

  <BackToTop />
</BaseLayout>

<style>
  /* ===============================
     HERO SECTION
     =============================== */
  .hero {
    padding: 8rem 0 6rem;
    background: linear-gradient(
      135deg,
      rgb(var(--color-background)) 0%,
      rgba(var(--color-primary), 0.05) 100%
    );
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: "";
    position: absolute;
    inset: 0;
    background: radial-gradient(
        circle at 20% 80%,
        rgba(var(--color-primary), 0.08) 0%,
        transparent 50%
      ),
      radial-gradient(
        circle at 80% 20%,
        rgba(var(--color-accent), 0.05) 0%,
        transparent 50%
      );
    pointer-events: none;
  }

  .hero .container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 5rem;
    align-items: center;
    position: relative;
    z-index: 1;
  }

  .hero-title {
    font-family: var(--font-hero);
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 700;
    line-height: 1.15;
    margin-bottom: 2rem;
    color: rgb(var(--color-text));
  }

  .hero-subtitle {
    font-size: 1.25rem;
    line-height: 1.7;
    color: rgb(var(--color-text-muted));
    margin-bottom: 2rem;
  }

  .hero-badge {
    display: inline-flex;
    padding: 0.75rem 1.5rem;
    background: rgba(var(--color-accent), 0.15);
    border: 1px solid rgba(var(--color-accent), 0.3);
    border-radius: var(--radius-lg);
    color: rgb(var(--color-accent));
    font-size: 0.875rem;
    font-weight: 500;
    margin-bottom: 2rem;
  }

  .hero-cta {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  /* Chat Preview */
  .chat-preview {
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow-xl);
    background: rgb(var(--color-surface));
    border: 1px solid rgba(var(--color-border), 0.3);
  }

  .chat-header {
    display: flex;
    align-items: center;
    padding: 1.25rem 1.5rem;
    gap: 1rem;
    background: rgba(var(--color-surface-variant), 0.3);
    border-bottom: 1px solid rgba(var(--color-border), 0.2);
  }

  .agent-avatar {
    font-size: 2rem;
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    background: linear-gradient(
      135deg,
      rgb(var(--color-primary)),
      rgb(var(--color-primary-hover))
    );
  }

  .agent-info {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .agent-name {
    font-weight: 600;
    font-size: 1rem;
    color: rgb(var(--color-text));
  }

  .agent-status {
    font-size: 0.875rem;
    color: rgb(var(--color-text-muted));
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .agent-status::before {
    content: "●";
    color: rgb(var(--color-success));
    font-size: 0.75rem;
  }

  .chat-messages {
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    min-height: 320px;
    background: rgba(var(--color-surface-variant), 0.1);
  }

  .message {
    display: flex;
    gap: 0.75rem;
    max-width: 90%;
    animation: slideIn 0.3s ease;
  }

  .user-message {
    align-self: flex-end;
    flex-direction: row-reverse;
  }

  .agent-message {
    align-self: flex-start;
  }

  .message-avatar {
    font-size: 1.5rem;
    flex-shrink: 0;
  }

  .message-content {
    padding: 1rem 1.25rem;
    border-radius: var(--radius-lg);
    font-size: 0.95rem;
    line-height: 1.6;
    flex: 1;
    text-align: left;
  }

  .user-message .message-content {
    background: rgb(var(--color-primary));
    color: rgb(var(--color-text-button));
    border-bottom-right-radius: 0.25rem;
  }

  .agent-message .message-content {
    background: rgba(var(--color-surface-variant), 0.8);
    color: rgb(var(--color-text));
    border-bottom-left-radius: 0.25rem;
    border: 1px solid rgba(var(--color-border), 0.7);
  }

  .inline-code {
    background: rgba(0, 0, 0, 0.2);
    padding: 0.25rem 0.5rem;
    border-radius: var(--radius-sm);
    font-family: var(--font-mono);
    font-size: 0.875rem;
    display: inline-block;
    text-align: left;
  }

  /* ===============================
   TECH BADGES (Hero)
   =============================== */
  .tech-badges {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
    margin-bottom: 2rem;
  }

  .tech-badges .badge {
    padding: 0.5rem 1rem;
    background: rgba(var(--color-surface-variant), 0.5);
    border: 1px solid rgba(var(--color-border), 0.5);
    border-radius: var(--radius-md);
    color: rgb(var(--color-text));
    font-size: 0.875rem;
    font-weight: 500;
  }

  /* ===============================
   CONTEXT SECTION
   =============================== */
  .context-section {
    background: rgb(var(--color-surface));
  }

  .context-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .context-card {
    background: rgba(var(--color-surface-variant), 0.3);
    border: 1px solid rgba(var(--color-border), 0.3);
    border-radius: var(--radius-lg);
    padding: 2.5rem;
  }

  .context-card--solution {
    background: rgba(var(--color-primary), 0.03);
    border: 1px solid rgba(var(--color-primary), 0.2);
  }

  .context-card h3 {
    font-family: var(--font-heading);
    font-size: 1.5rem;
    color: rgb(var(--color-text));
    margin-bottom: 1.5rem;
  }

  .context-card ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .context-card ul li {
    padding-left: 1.75rem;
    margin-bottom: 1rem;
    position: relative;
    color: rgb(var(--color-text-muted));
    line-height: 1.6;
  }

  .context-card ul li::before {
    content: "•";
    position: absolute;
    left: 0;
    color: rgb(var(--color-primary));
    font-size: 1.5rem;
    line-height: 1;
  }

  .context-card--solution ul li {
    padding-left: 2rem;
  }

  .context-card--solution ul li::before {
    content: "→";
    font-size: 1rem;
    line-height: 1.6;
  }

  .context-card strong {
    color: rgb(var(--color-text));
    font-weight: 600;
  }

  .context-result {
    text-align: center;
    padding: 2rem 2.5rem;
    background: rgba(var(--color-primary), 0.05);
    border: 2px solid rgba(var(--color-primary), 0.3);
    border-radius: var(--radius-lg);
    max-width: 900px;
    margin: 0 auto;
  }

  .context-result p {
    font-size: 1.125rem;
    color: rgb(var(--color-text));
    margin: 0;
    line-height: 1.7;
  }

  .context-result strong {
    color: rgb(var(--color-primary));
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .context-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 768px) {
    .context-card {
      padding: 1.5rem;
    }

    .context-result {
      padding: 1.5rem;
    }
  }

  /* ===============================
   USE CASES & METRICS
   =============================== */
  .use-cases {
    margin-top: 4rem;
    padding: 2.5rem;
    background: rgba(var(--color-surface-variant), 0.3);
    border: 1px solid rgba(var(--color-border), 0.3);
    border-radius: var(--radius-lg);
  }

  .use-cases h3 {
    font-family: var(--font-heading);
    font-size: 1.5rem;
    color: rgb(var(--color-text));
    margin-bottom: 1.5rem;
    text-align: center;
  }

  .use-cases-list {
    list-style: none;
    padding: 0;
    max-width: 700px;
    margin: 0 auto 2rem;
  }

  .use-cases-list li {
    padding-left: 1.5rem;
    margin-bottom: 0.75rem;
    position: relative;
    color: rgb(var(--color-text-muted));
    font-size: 0.95rem;
  }

  .use-cases-list li::before {
    content: "→";
    position: absolute;
    left: 0;
    color: rgb(var(--color-primary));
    font-weight: bold;
  }

  .metrics-text {
    text-align: center;
    color: rgb(var(--color-text));
    font-size: 1rem;
    margin: 0;
  }

  /* ===============================
   APPROACH SECTION
   =============================== */
  .approach-section {
    background: rgb(var(--color-surface));
  }

  .approach-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
  }

  .approach-card {
    background: rgba(var(--color-surface-variant), 0.3);
    border: 1px solid rgba(var(--color-border), 0.3);
    border-radius: var(--radius-lg);
    padding: 2rem;
  }

  .approach-card h4 {
    font-family: var(--font-heading);
    font-size: 1.125rem;
    color: rgb(var(--color-text));
    margin-bottom: 0.75rem;
  }

  .approach-card p {
    color: rgb(var(--color-text-muted));
    font-size: 0.95rem;
    margin: 0;
  }

  /* ===============================
   OFFERS - SIMPLIFIED
   =============================== */
  .offer-tagline {
    font-size: 0.95rem;
    color: rgb(var(--color-text-muted));
    margin-bottom: 1rem;
    font-weight: 400;
  }

  .offer-use-cases {
    margin-top: 1.5rem;
    padding: 1rem;
    background: rgba(var(--color-accent), 0.05);
    border-radius: var(--radius-md);
    font-size: 0.9rem;
  }

  .offer-use-cases p {
    margin: 0;
    color: rgb(var(--color-text-muted));
  }

  .offer-use-cases strong {
    color: rgb(var(--color-text));
  }

  /* ===============================
   RESPONSIVE
   =============================== */
  @media (max-width: 1024px) {
    .approach-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 768px) {
    .tech-badges {
      justify-content: center;
    }

    .use-cases {
      padding: 1.5rem;
    }

    .approach-card {
      padding: 1.5rem;
    }
  }

  /* ===============================
   SECTIONS COMMUNES
   =============================== */
  .section-title {
    font-family: var(--font-heading);
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 700;
    text-align: center;
    margin-bottom: 3rem;
    color: rgb(var(--color-text));
  }

  .section-intro {
    text-align: center;
    font-size: 1.125rem;
    color: rgb(var(--color-text-muted));
    max-width: 800px;
    margin: 0 auto 3rem;
  }

  /* ===============================
     BLOC SOLUTION
     =============================== */
  .solution-section {
    background: rgba(var(--color-surface-variant), 0.5);
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .feature-card {
    background: rgb(var(--color-surface));
    border: 1px solid rgba(var(--color-border), 0.3);
    border-radius: var(--radius-lg);
    padding: 2rem;
    text-align: center;
    transition:
      transform 0.2s ease,
      box-shadow 0.2s ease;
  }

  .feature-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-xl);
  }

  .feature-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
    color: rgb(var(--color-primary));
    font-weight: 300;
    line-height: 1;
  }

  .feature-card h3 {
    font-family: var(--font-heading);
    font-size: 1.25rem;
    color: rgb(var(--color-text));
    margin-bottom: 1rem;
  }

  .feature-card p {
    color: rgb(var(--color-text-muted));
    font-size: 0.95rem;
  }

  .solution-result {
    text-align: center;
    padding: 2rem;
    background: rgba(var(--color-primary), 0.05);
    border: 2px solid rgba(var(--color-primary), 0.3);
    border-radius: var(--radius-lg);
  }

  .solution-result p {
    font-size: 1.125rem;
    color: rgb(var(--color-text));
    margin: 0;
  }

  .solution-result strong {
    color: rgb(var(--color-primary));
  }

  /* ===============================
     BLOC OFFRES
     =============================== */
  .offers-section {
    background: rgba(var(--color-surface-variant), 0.5);
  }

  .offers-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    margin-bottom: 3rem;
    align-items: start;
  }

  .offer-card {
    background: rgb(var(--color-surface));
    border: 1px solid rgba(var(--color-border), 0.3);
    border-radius: var(--radius-lg);
    padding: 2rem;
    position: relative;
    transition:
      transform 0.2s ease,
      box-shadow 0.2s ease;
    display: flex;
    flex-direction: column;
    height: 100%;
  }

  .offer-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-xl);
  }

  .offer-recommended {
    border: 2px solid rgb(var(--color-primary));
    box-shadow: 0 0 0 4px rgba(var(--color-primary), 0.1);
  }

  .offer-badge {
    position: absolute;
    top: -12px;
    right: 20px;
    background: linear-gradient(
      135deg,
      rgb(var(--color-primary)),
      rgb(var(--color-primary-hover))
    );
    color: rgb(var(--color-text-button));
    padding: 0.5rem 1rem;
    border-radius: var(--radius-lg);
    font-size: 0.875rem;
    font-weight: 600;
    box-shadow: var(--shadow-md);
  }

  .offer-header h3 {
    font-family: var(--font-heading);
    font-size: 1.75rem;
    color: rgb(var(--color-text));
    margin-bottom: 1rem;
  }

  .offer-price {
    font-size: 2.5rem;
    font-weight: 700;
    color: rgb(var(--color-primary));
    margin-bottom: 0.5rem;
  }

  .offer-vat {
    font-size: 0.75rem;
    color: rgb(var(--color-text-muted));
    font-style: italic;
    margin-bottom: 1.5rem;
  }

  .offer-for {
    background: rgba(var(--color-accent), 0.1);
    padding: 1rem;
    border-radius: var(--radius-md);
    margin-bottom: 1rem;
    font-size: 0.95rem;
    color: rgb(var(--color-text-muted));
  }

  .offer-for strong {
    color: rgb(var(--color-text));
  }

  .offer-subtitle {
    font-size: 1rem;
    color: rgb(var(--color-text));
    margin-bottom: 1rem;
    font-weight: 500;
  }

  .offer-duration {
    font-size: 0.9rem;
    color: rgb(var(--color-text-muted));
    margin-bottom: 1.5rem;
  }

  .offer-includes {
    flex: 1;
  }

  .offer-includes h4 {
    font-size: 1rem;
    color: rgb(var(--color-text));
    margin-bottom: 1rem;
  }

  .offer-includes ul {
    list-style: none;
    padding: 0;
    margin-bottom: 1.5rem;
  }

  .offer-includes ul li {
    padding-left: 1.5rem;
    margin-bottom: 0.75rem;
    position: relative;
    color: rgb(var(--color-text-muted));
    font-size: 0.95rem;
  }

  .offer-includes ul li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: rgb(var(--color-success));
    font-weight: bold;
  }

  .offer-cta {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    margin-top: auto;
    margin-bottom: 1.5rem;
  }

  .offer-cta .btn {
    width: 100%;
  }

  .offer-details {
    border-top: 1px solid rgba(var(--color-border), 0.3);
    padding-top: 1rem;
    margin-top: auto;
  }

  .offer-details summary {
    cursor: pointer;
    color: rgb(var(--color-primary));
    font-weight: 500;
    user-select: none;
    transition: opacity 0.2s ease;
  }

  .offer-details summary:hover {
    opacity: 0.8;
  }

  .offer-full-details {
    margin-top: 1rem;
    padding-top: 1rem;
    border-top: 1px solid rgba(var(--color-border), 0.2);
  }

  .offer-full-details p {
    margin-bottom: 0.75rem;
    color: rgb(var(--color-text));
  }

  .offer-full-details ul {
    list-style: none;
    padding: 0;
  }

  .offer-full-details ul li {
    padding-left: 1.5rem;
    margin-bottom: 0.5rem;
    position: relative;
    color: rgb(var(--color-text-muted));
    font-size: 0.9rem;
  }

  .offer-full-details ul li::before {
    content: "→";
    position: absolute;
    left: 0;
    color: rgb(var(--color-accent));
  }

  .offer-unsure {
    text-align: center;
    padding: 2rem;
    background: rgba(var(--color-primary), 0.05);
    border: 1px solid rgba(var(--color-primary), 0.2);
    border-radius: var(--radius-lg);
  }

  .offer-unsure p:first-child {
    font-size: 1.25rem;
    color: rgb(var(--color-text));
    font-weight: 600;
    margin-bottom: 0.75rem;
  }

  .offer-unsure p:last-of-type {
    color: rgb(var(--color-text-muted));
    margin-bottom: 1.5rem;
  }

  /* ===============================
   POC CARD - SPECIAL STYLING
   =============================== */

  /* Grid avec POC (4 colonnes) */
  .offers-grid--with-poc {
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
  }

  /* POC Card - Design distinct */
  .offer-card--poc {
    background: rgba(var(--color-accent), 0.03);
    border: 2px dashed rgba(var(--color-accent), 0.4);
  }

  .offer-card--poc:hover {
    border-color: rgba(var(--color-accent), 0.6);
    background: rgba(var(--color-accent), 0.05);
  }

  /* POC Badge */
  .offer-badge--poc {
    background: linear-gradient(
      135deg,
      rgb(var(--color-accent)),
      rgba(var(--color-accent), 0.8)
    );
  }

  /* Upgrade highlight */
  .offer-upgrade {
    margin-top: 1.5rem;
    padding: 1rem;
    background: rgba(var(--color-success), 0.08);
    border: 1px solid rgba(var(--color-success), 0.3);
    border-radius: var(--radius-md);
  }

  .offer-upgrade-text {
    margin: 0;
    font-size: 0.9rem;
    color: rgb(var(--color-text));
    text-align: center;
  }

  .offer-upgrade-text strong {
    color: rgb(var(--color-success));
  }

  /* Prix après POC */
  .offer-after-poc {
    font-size: 0.95rem;
    color: rgb(var(--color-success));
    margin-top: 0.5rem;
    font-weight: 600;
  }

  /* ===============================
   RESPONSIVE - POC
   =============================== */

  @media (max-width: 1400px) {
    .offers-grid--with-poc {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 1024px) {
    .offers-grid--with-poc {
      grid-template-columns: 1fr;
    }

    .offer-card--poc {
      order: -1; /* POC en premier sur mobile */
    }
  }

  /* ===============================
     SERVICES COMPLÉMENTAIRES
     =============================== */
  .complementary-services {
    background: rgb(var(--color-surface));
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
  }

  .service-card {
    background: rgba(var(--color-surface-variant), 0.3);
    border: 1px solid rgba(var(--color-border), 0.3);
    border-radius: var(--radius-lg);
    padding: 2rem;
  }

  .service-icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    color: rgb(var(--color-primary));
  }

  .service-card h3 {
    font-family: var(--font-heading);
    font-size: 1.5rem;
    color: rgb(var(--color-text));
    margin-bottom: 1rem;
  }

  .service-price {
    font-size: 2rem;
    font-weight: 700;
    color: rgb(var(--color-primary));
    margin-bottom: 0.5rem;
  }

  .service-vat {
    font-size: 0.75rem;
    color: rgb(var(--color-text-muted));
    font-style: italic;
    margin-bottom: 1rem;
  }

  .service-includes h4 {
    font-size: 1rem;
    color: rgb(var(--color-text));
    margin-bottom: 1rem;
    margin-top: 1.5rem;
  }

  .service-includes ul {
    list-style: none;
    padding: 0;
    margin-bottom: 1.5rem;
  }

  .service-includes ul li {
    padding-left: 1.5rem;
    margin-bottom: 0.75rem;
    position: relative;
    color: rgb(var(--color-text-muted));
    font-size: 0.95rem;
  }

  .service-includes ul li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: rgb(var(--color-success));
    font-weight: bold;
  }

  .service-duration {
    font-size: 0.9rem;
    color: rgb(var(--color-text-muted));
    margin-bottom: 1.5rem;
  }

  .maintenance-options {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    margin-top: 1.5rem;
    margin-bottom: 1.5rem;
  }

  .maintenance-option {
    background: rgba(var(--color-surface-variant), 0.5);
    padding: 1.5rem;
    border-radius: var(--radius-md);
    border: 1px solid rgba(var(--color-border), 0.3);
  }

  .maintenance-option h4 {
    color: rgb(var(--color-text));
    margin-bottom: 1rem;
    font-size: 1.125rem;
  }

  .maintenance-option ul {
    list-style: none;
    padding: 0;
  }

  .maintenance-option ul li {
    padding-left: 1.5rem;
    margin-bottom: 0.5rem;
    position: relative;
    color: rgb(var(--color-text-muted));
    font-size: 0.9rem;
  }

  .maintenance-option ul li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: rgb(var(--color-success));
  }

  /* ===============================
     BLOC PREUVE
     =============================== */
  .proof-section {
    background: rgba(var(--color-surface-variant), 0.5);
  }

  .proof-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: center;
    margin-bottom: 4rem;
  }

  .proof-image img {
    width: 100%;
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-xl);
    border: 1px solid rgba(var(--color-border), 0.3);
  }

  .proof-text p {
    color: rgb(var(--color-text-muted));
    margin-bottom: 1.5rem;
  }

  .proof-text h4 {
    color: rgb(var(--color-text));
    margin-bottom: 1rem;
    margin-top: 2rem;
  }

  .tech-stack {
    list-style: none;
    padding: 0;
    margin-bottom: 2rem;
  }

  .tech-stack li {
    padding-left: 1.5rem;
    margin-bottom: 0.75rem;
    position: relative;
    color: rgb(var(--color-text-muted));
  }

  .tech-stack li::before {
    content: "▸";
    position: absolute;
    left: 0;
    color: rgb(var(--color-primary));
  }

  .tech-stack strong {
    color: rgb(var(--color-text));
  }

  .portfolio-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 2rem;
  }

  /* ===============================
     CTA FINAL
     =============================== */
  .cta-final {
    background: linear-gradient(
      135deg,
      rgba(var(--color-primary), 0.08) 0%,
      rgba(var(--color-accent), 0.05) 100%
    );
    position: relative;
    overflow: hidden;
  }

  .cta-final::before {
    content: "";
    position: absolute;
    inset: 0;
    background: radial-gradient(
      circle at 30% 70%,
      rgba(var(--color-primary), 0.1) 0%,
      transparent 50%
    );
    pointer-events: none;
  }

  .cta-content {
    text-align: center;
    max-width: 700px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  .cta-content h2 {
    font-family: var(--font-heading);
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 700;
    margin-bottom: 1.5rem;
    color: rgb(var(--color-text));
  }

  .cta-content p {
    font-size: 1.125rem;
    color: rgb(var(--color-text-muted));
    margin-bottom: 1rem;
  }

  .cta-no-bs {
    color: rgb(var(--color-primary)) !important;
    font-size: 1rem !important;
    margin-bottom: 2rem !important;
  }

  .cta-note {
    color: rgb(var(--color-text)) !important;
    font-size: 1rem !important;
    margin-bottom: 2rem !important;
    font-weight: 500;
  }

  /* ===============================
     RESPONSIVE
     =============================== */
  @media (max-width: 1024px) {
    .hero .container,
    .proof-content {
      grid-template-columns: 1fr;
      gap: 3rem;
      text-align: center;
    }

    .hero-cta {
      justify-content: center;
    }

    .chat-preview {
      max-width: 600px;
      margin: 0 auto;
    }

    .offers-grid,
    .differentiation-points,
    .features-grid {
      grid-template-columns: 1fr;
    }

    .problem-grid,
    .services-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding: 5rem 0 4rem;
    }

    .section-spacing {
      padding: 3rem 0;
    }

    .comparison-table {
      font-size: 0.8rem;
    }

    .comparison-table th,
    .comparison-table td {
      padding: 0.75rem 0.5rem;
    }

    .offer-card {
      padding: 1.5rem;
    }

    .message {
      max-width: 95%;
    }

    .message-avatar {
      font-size: 1.25rem;
    }
  }

  @media (max-width: 600px) {
    .hero-title {
      font-size: 2rem;
    }

    .hero-subtitle {
      font-size: 1rem;
    }

    .section-title {
      font-size: 1.75rem;
    }

    .btn {
      width: 100%;
    }

    .hero-cta .btn,
    .offer-cta .btn {
      font-size: 0.9rem;
      padding: 0.75rem 1.25rem;
    }

    .chat-messages {
      min-height: 280px;
      padding: 1rem;
    }

    .comparison-table th,
    .comparison-table td {
      padding: 0.5rem 0.25rem;
      font-size: 0.75rem;
    }
  }
</style>
