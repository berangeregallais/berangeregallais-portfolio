---
// src/pages/fr/portfolio/visionary-ai-agent.astro
import BaseLayout from "../../../layouts/BaseLayout.astro";
---

<BaseLayout
  title="Visionary AI Agent | Assistant conversationnel pour API d'analyse d'images"
  description="Agent conversationnel intelligent pour assistance technique Visionary.ai. Interface React, intégration Gemini, base de connaissances structurée."
  lang="fr"
>
  <!-- Hero Section -->
  <section class="project-hero">
    <div class="container">
      <div class="hero-back">
        <a href="/fr/portfolio" class="back-link">← Retour au portfolio</a>
      </div>
      <div class="hero-content">
        <div class="hero-badge">Agent conversationnel</div>
        <h1>Visionary AI Agent</h1>
        <p class="hero-subtitle">
          Agent conversationnel spécialisé dans l'assistance technique pour
          l'API d'analyse d'images Visionary.ai. Interface moderne, réponses
          contextuelles, et intégration Gemini.
        </p>
        <div class="hero-image">
          <img
            src="/portfolio/visionary-ai-agent.png"
            alt="Interface Visionary AI Agent"
          />
        </div>
      </div>
    </div>
  </section>

  <!-- Vue d'ensemble -->
  <section class="overview-section">
    <div class="container">
      <h2 class="section-title">Vue d'ensemble</h2>
      <div class="overview-grid">
        <div class="overview-card">
          <div class="overview-icon">🎯</div>
          <h3>Objectif</h3>
          <p>
            Transformer la documentation statique en conversation intelligente
            et contextuelle pour aider les développeurs à intégrer rapidement
            l'API Visionary.ai.
          </p>
        </div>
        <div class="overview-card">
          <div class="overview-icon">⚙️</div>
          <h3>Solution</h3>
          <p>
            Système hybride combinant base de connaissances structurée et
            génération via Gemini, avec interface de chat moderne et fallbacks
            robustes.
          </p>
        </div>
        <div class="overview-card">
          <div class="overview-icon">🚀</div>
          <h3>Résultats</h3>
          <p>
            Démonstration fonctionnelle avec réponses instantanées et
            contextualisées, prête à être adaptée à d'autres APIs et cas
            d'usage.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Démonstration en direct -->
  <section class="demo-section">
    <div class="container">
      <h2 class="section-title">Démonstration en direct</h2>
      <p class="section-intro">
        L'agent est disponible en ligne et prêt à être testé. Essayez-le
        directement sur la démo hébergée.
      </p>

      <div class="demo-notice">
        <div class="demo-notice-icon">ℹ️</div>
        <div class="demo-notice-content">
          <strong>Version démo limitée</strong>
          <p>
            La démo publique fonctionne avec des réponses pré-écrites pour limiter
            les coûts API et éviter les abus. L'agent peut fonctionner en <span
              class="highlight-text">mode live avec Gemini AI</span
            > pour des réponses dynamiques et contextuelles (voir section <a
              href="#modes-fonctionnement">Modes de fonctionnement</a
            >).
          </p>
        </div>
      </div>

      <div class="demo-container">
        <div class="demo-content">
          <div class="demo-info">
            <h3>Testez l'agent maintenant</h3>
            <p>
              L'agent Visionary AI est hébergé et fonctionnel. Vous pouvez
              l'essayer immédiatement pour découvrir ses capacités en action.
            </p>
            <ul class="demo-features">
              <li>Tour guidé interactif en 4 étapes</li>
              <li>Test d'images d'exemple en un clic</li>
              <li>Plus de 8 catégories de réponses (auth, endpoints, SDKs, etc.)</li>
              <li>
                Exemples de code multi-langages (React, Node.js, Python, Swift,
                Kotlin)
              </li>
              <li>Interface animée avec effets de transition fluides</li>
              <li>Mode sombre/clair avec transitions</li>
            </ul>
            <a
              href="https://visionary-ai-agent.netlify.app/"
              target="_blank"
              rel="noopener noreferrer"
              class="btn btn-primary btn-large demo-button"
            >
              Tester la démo en direct →
            </a>
          </div>
          <div class="demo-image-wrapper">
            <img
              src="/portfolio/visionary-ai-agent.png"
              alt="Interface Visionary AI Agent"
              class="demo-image"
            />
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Fonctionnalités principales -->
  <section class="features-section">
    <div class="container">
      <h2 class="section-title">Fonctionnalités principales</h2>
      <div class="features-grid">
        <div class="feature-item">
          <div class="feature-icon">🎯</div>
          <h3>Expérience démo enrichie</h3>
          <p>
            Réponses contextuelles avec exemples de code, plus de 8 catégories
            (authentification, endpoints, SDKs, performance, intégration),
            exemples pratiques pour React, Node.js, Python.
          </p>
        </div>
        <div class="feature-item">
          <div class="feature-icon">🖼️</div>
          <h3>Test d'images d'exemple</h3>
          <p>
            Exemples d'images cliquables, analyses instantanées, scénarios
            multiples (portrait, paysage, scènes urbaines), prévisualisations
            visuelles avec effets hover.
          </p>
        </div>
        <div class="feature-item">
          <div class="feature-icon">🎨</div>
          <h3>UI/UX avancée</h3>
          <p>
            Animations fluides, boutons interactifs, copy-to-clipboard avec
            feedback, notifications toast, design responsive mobile-first.
          </p>
        </div>
        <div class="feature-item">
          <div class="feature-icon">🎓</div>
          <h3>Onboarding guidé</h3>
          <p>
            Tour interactif en 4 étapes, introduction progressive des
            fonctionnalités, option de saut, indicateurs visuels avec points de
            progression.
          </p>
        </div>
        <div class="feature-item">
          <div class="feature-icon">📊</div>
          <h3>Données démo riches</h3>
          <p>
            Plus de 8 catégories de réponses détaillées, exemples de code prêts
            à l'emploi, cas d'usage pratiques, conseils d'optimisation de
            performance.
          </p>
        </div>
        <div class="feature-item">
          <div class="feature-icon">⚡</div>
          <h3>Performance optimisée</h3>
          <p>
            Bundle optimisé avec code splitting, animations accélérées
            matériellement, rendu React optimisé, cache intelligent des
            réponses.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Stack Technique -->
  <section class="stack-section">
    <div class="container">
      <h2 class="section-title">Stack Technique</h2>
      <div class="stack-grid">
        <div class="stack-category">
          <h3>Frontend</h3>
          <ul>
            <li>
              <strong>React 18 + TypeScript</strong> — Développement type-safe
            </li>
            <li>
              <strong>Tailwind CSS</strong> — Design system utility-first avec animations
              personnalisées
            </li>
            <li><strong>Vite</strong> — Build tool rapide et optimisé</li>
            <li><strong>Custom Hooks</strong> — Gestion d'état réactive</li>
          </ul>
        </div>
        <div class="stack-category">
          <h3>Backend & IA</h3>
          <ul>
            <li>
              <strong>Google Gemini 2.5 Flash API</strong> — Génération contextuelle
            </li>
            <li>
              <strong>Netlify Functions</strong> — Architecture serverless
            </li>
            <li>
              <strong>Mode démo</strong> — Configuration par environnement
            </li>
          </ul>
        </div>
        <div class="stack-category">
          <h3>Fonctionnalités</h3>
          <ul>
            <li>Design responsive mobile-first</li>
            <li>Thème sombre/clair avec transitions</li>
            <li>Conversation en temps réel avec contexte</li>
            <li>Parsing markdown avancé avec syntax highlighting</li>
            <li>Gestion d'erreurs robuste et fallbacks</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- Modes de fonctionnement -->
  <section id="modes-fonctionnement" class="modes-section">
    <div class="container">
      <h2 class="section-title">Modes de fonctionnement</h2>
      <p class="section-intro">
        L'architecture supporte deux modes pour s'adapter à vos besoins : mode
        démo pour les démonstrations publiques, et mode live avec Gemini AI pour
        des réponses dynamiques.
      </p>

      <div class="modes-grid">
        <div class="mode-card mode-card--demo">
          <div class="mode-header">
            <div class="mode-icon">🎯</div>
            <h3>Mode Démo</h3>
            <span class="mode-badge mode-badge--free">Gratuit</span>
          </div>
          <div class="mode-content">
            <p class="mode-description">
              Réponses pré-écrites optimisées et professionnelles, parfaites
              pour les démonstrations publiques.
            </p>
            <ul class="mode-features">
              <li>Réponses instantanées (0 latence)</li>
              <li>Plus de 8 catégories de réponses pré-écrites</li>
              <li>Exemples de code prêts à l'emploi</li>
              <li>Aucun coût d'API</li>
              <li>Fonctionne pour tous les visiteurs</li>
            </ul>
            <div class="mode-examples">
              <strong>Exemples :</strong>
              <ul>
                <li>"mountain" → Réponse spécifique paysages montagneux</li>
                <li>"portrait" → Réponse spécifique analyse d'émotions</li>
                <li>"analyze" → Réponse générale détection d'objets</li>
              </ul>
            </div>
            <div class="mode-use-case">
              <strong>Idéal pour :</strong> Portfolio public, démonstrations, présentation
              de capacités
            </div>
          </div>
        </div>

        <div class="mode-card mode-card--live">
          <div class="mode-header">
            <div class="mode-icon">🚀</div>
            <h3>Mode Live (Gemini AI)</h3>
            <span class="mode-badge mode-badge--ai">IA Dynamique</span>
          </div>
          <div class="mode-content">
            <p class="mode-description">
              Réponses générées dynamiquement par Gemini AI, adaptées au
              contexte exact de chaque question.
            </p>
            <ul class="mode-features">
              <li>Réponses contextuelles adaptées</li>
              <li>Compréhension avancée du contexte</li>
              <li>Suggestions personnalisées</li>
              <li>Réponses en quelques secondes</li>
              <li>Personnalisation totale</li>
            </ul>
            <div class="mode-config">
              <strong>Configuration :</strong>
              <code>DEMO_MODE=false</code><br />
              <code>GEMINI_API_KEY=votre_cle</code>
            </div>
            <div class="mode-use-case">
              <strong>Idéal pour :</strong> Tests personnels, intégrations spécifiques,
              personnalisation avancée
            </div>
          </div>
        </div>
      </div>

  <!-- CTA Section -->
  <section class="cta-section">
    <div class="container">
      <div class="cta-content">
        <h2>Envie d'un agent similaire pour votre API&nbsp;?</h2>
        <p>
          Ce concept est réplicable et personnalisable. Discutons de votre cas
          d'usage et des intégrations nécessaires.
        </p>
        <div class="cta-buttons">
          <a
            href="https://visionary-ai-agent.netlify.app/"
            target="_blank"
            rel="noopener noreferrer"
            class="btn btn-primary btn-large">Voir la démo</a
          >
          <a
            href="https://github.com/berangeregallais/visionary-ai-docs-assistant"
            target="_blank"
            rel="noopener noreferrer"
            class="btn btn-secondary">Voir le code sur GitHub</a
          >
          <a href="/fr/contact" class="btn btn-secondary"
            >Discuter de votre projet</a
          >
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  :root {
    --section-spacing: 6rem;
  }

  .project-hero {
    padding: 4rem 0 6rem;
    background: linear-gradient(
      135deg,
      rgb(var(--color-background)) 0%,
      rgba(var(--color-accent), 0.06) 100%
    );
  }

  .hero-back {
    margin-bottom: 2rem;
  }

  .back-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: rgb(var(--color-text-muted));
    text-decoration: none;
    font-size: 0.95rem;
    transition: color 0.2s ease;
  }
  .back-link:hover {
    color: rgb(var(--color-primary));
  }

  .hero-badge {
    display: inline-block;
    padding: 0.5rem 1rem;
    background: linear-gradient(
      135deg,
      rgb(var(--color-accent)),
      rgba(var(--color-accent), 0.8)
    );
    color: rgb(var(--color-text-button));
    border-radius: var(--radius-md);
    font-size: 0.875rem;
    font-weight: 600;
    margin-bottom: 1.5rem;
  }

  .project-hero h1 {
    font-family: var(--font-heading);
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 700;
    margin-bottom: 1.5rem;
    color: rgb(var(--color-text));
  }

  .hero-subtitle {
    font-size: 1.25rem;
    line-height: 1.7;
    color: rgb(var(--color-text-muted));
    max-width: 800px;
    margin-bottom: 3rem;
  }

  .hero-image {
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow-xl);
    border: 1px solid rgba(var(--color-border), 0.3);
  }
  .hero-image img {
    width: 100%;
    display: block;
  }

  .overview-section {
    padding: var(--section-spacing) 0;
    background: rgb(var(--color-surface));
  }
  .overview-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }
  .overview-card {
    background: rgba(var(--color-surface-variant), 0.3);
    border: 1px solid rgba(var(--color-border), 0.3);
    border-radius: var(--radius-lg);
    padding: 2rem;
    text-align: center;
  }
  .overview-icon {
    font-size: 2rem;
    margin-bottom: 1rem;
  }

  .demo-section {
    padding: var(--section-spacing) 0;
  }
  .section-title {
    font-family: var(--font-heading);
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 700;
    text-align: center;
    margin-bottom: 1.5rem;
    color: rgb(var(--color-text));
  }
  .section-intro {
    text-align: center;
    font-size: 1.125rem;
    color: rgb(var(--color-text-muted));
    max-width: 700px;
    margin: 0 auto 3rem;
  }

  .demo-notice {
    max-width: 1000px;
    margin: 0 auto 2rem;
    background: rgba(var(--color-primary), 0.1);
    border: 2px solid rgba(var(--color-primary), 0.3);
    border-radius: var(--radius-lg);
    padding: 1.5rem;
    display: flex;
    gap: 1rem;
    align-items: flex-start;
  }
  .demo-notice-icon {
    font-size: 1.5rem;
    flex-shrink: 0;
  }
  .demo-notice-content {
    flex: 1;
  }
  .demo-notice-content strong {
    display: block;
    color: rgb(var(--color-text));
    font-size: 1.1rem;
    margin-bottom: 0.5rem;
  }
  .demo-notice-content p {
    color: rgb(var(--color-text-muted));
    line-height: 1.6;
    margin: 0;
    font-size: 0.95rem;
  }
  .demo-notice-content .highlight-text {
    color: rgb(var(--color-primary));
    font-weight: 700;
  }
  .demo-notice-content a {
    color: rgb(var(--color-primary));
    text-decoration: underline;
    text-decoration-color: rgba(var(--color-primary), 0.4);
    transition: text-decoration-color 0.2s ease;
  }
  .demo-notice-content a:hover {
    text-decoration-color: rgb(var(--color-primary));
  }

  .demo-container {
    max-width: 1000px;
    margin: 0 auto;
  }
  .demo-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: center;
    background: rgba(var(--color-surface-variant), 0.3);
    border: 1px solid rgba(var(--color-border), 0.3);
    border-radius: var(--radius-lg);
    padding: 3rem;
  }
  .demo-info h3 {
    font-size: 1.75rem;
    color: rgb(var(--color-text));
    margin-bottom: 1rem;
  }
  .demo-info p {
    color: rgb(var(--color-text-muted));
    line-height: 1.7;
    margin-bottom: 2rem;
  }
  .demo-features {
    list-style: none;
    padding: 0;
    margin: 0 0 2rem 0;
  }
  .demo-features li {
    padding-left: 1.5rem;
    margin-bottom: 0.75rem;
    position: relative;
    color: rgb(var(--color-text-muted));
    font-size: 0.95rem;
  }
  .demo-features li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: rgb(var(--color-success));
    font-weight: bold;
  }
  .demo-button {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
  }
  .demo-image-wrapper {
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow-xl);
    border: 1px solid rgba(var(--color-border), 0.3);
  }
  .demo-image {
    width: 100%;
    height: auto;
    display: block;
  }

  .features-section {
    padding: var(--section-spacing) 0;
    background: rgb(var(--color-surface));
  }
  .features-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }
  .feature-item {
    background: rgba(var(--color-surface-variant), 0.3);
    border: 1px solid rgba(var(--color-border), 0.3);
    border-radius: var(--radius-lg);
    padding: 2rem;
    transition:
      transform 0.2s ease,
      box-shadow 0.2s ease;
  }
  .feature-item:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-xl);
  }
  .feature-icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
  }
  .feature-item h3 {
    font-size: 1.25rem;
    color: rgb(var(--color-text));
    margin-bottom: 1rem;
  }
  .feature-item p {
    color: rgb(var(--color-text-muted));
    line-height: 1.6;
    font-size: 0.95rem;
  }

  .stack-section {
    padding: var(--section-spacing) 0;
  }
  .stack-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }
  .stack-category {
    background: rgba(var(--color-surface-variant), 0.3);
    border: 1px solid rgba(var(--color-border), 0.3);
    border-radius: var(--radius-lg);
    padding: 2rem;
  }
  .stack-category h3 {
    font-size: 1.5rem;
    color: rgb(var(--color-primary));
    margin-bottom: 1.5rem;
    border-bottom: 2px solid rgba(var(--color-primary), 0.3);
    padding-bottom: 0.75rem;
  }
  .stack-category ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .stack-category ul li {
    padding: 0.75rem 0;
    border-bottom: 1px solid rgba(var(--color-border), 0.2);
    color: rgb(var(--color-text-muted));
    line-height: 1.6;
  }
  .stack-category ul li:last-child {
    border-bottom: none;
  }
  .stack-category strong {
    color: rgb(var(--color-text));
  }

  .modes-section {
    padding: var(--section-spacing) 0;
    background: linear-gradient(
      135deg,
      rgba(var(--color-primary), 0.05) 0%,
      rgba(var(--color-accent), 0.03) 100%
    );
  }
  .modes-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
    margin-bottom: 3rem;
  }
  .mode-card {
    background: rgb(var(--color-surface));
    border: 2px solid rgba(var(--color-border), 0.3);
    border-radius: var(--radius-lg);
    padding: 2rem;
    transition:
      transform 0.2s ease,
      box-shadow 0.2s ease;
  }
  .mode-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-xl);
  }
  .mode-card--demo {
    border-color: rgba(var(--color-accent), 0.5);
  }
  .mode-card--live {
    border-color: rgba(var(--color-primary), 0.5);
  }
  .mode-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }
  .mode-icon {
    font-size: 2.5rem;
  }
  .mode-header h3 {
    font-size: 1.5rem;
    color: rgb(var(--color-text));
    margin: 0;
    flex: 1;
  }
  .mode-badge {
    padding: 0.375rem 0.75rem;
    border-radius: var(--radius-sm);
    font-size: 0.75rem;
    font-weight: 600;
  }
  .mode-badge--free {
    background: rgba(var(--color-success), 0.2);
    color: rgb(var(--color-success));
  }
  .mode-badge--ai {
    background: rgba(var(--color-primary), 0.2);
    color: rgb(var(--color-primary));
  }
  .mode-content {
    color: rgb(var(--color-text-muted));
  }
  .mode-description {
    font-size: 1rem;
    line-height: 1.6;
    margin-bottom: 1.5rem;
  }
  .mode-features {
    list-style: none;
    padding: 0;
    margin: 0 0 1.5rem 0;
  }
  .mode-features li {
    padding-left: 1.5rem;
    margin-bottom: 0.5rem;
    position: relative;
    font-size: 0.95rem;
  }
  .mode-features li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: rgb(var(--color-success));
    font-weight: bold;
  }
  .mode-examples,
  .mode-config,
  .mode-use-case {
    margin-top: 1.5rem;
    padding: 1rem;
    background: rgba(var(--color-surface-variant), 0.3);
    border-radius: var(--radius-md);
    font-size: 0.9rem;
  }
  .mode-examples strong,
  .mode-config strong,
  .mode-use-case strong {
    color: rgb(var(--color-text));
    display: block;
    margin-bottom: 0.5rem;
  }
  .mode-examples ul {
    list-style: none;
    padding: 0;
    margin: 0.5rem 0 0 0;
  }
  .mode-examples ul li {
    padding-left: 1rem;
    margin-bottom: 0.25rem;
    position: relative;
  }
  .mode-examples ul li::before {
    content: "→";
    position: absolute;
    left: 0;
    color: rgb(var(--color-primary));
  }
  .mode-config code {
    display: inline-block;
    background: rgba(var(--color-background), 0.5);
    padding: 0.25rem 0.5rem;
    border-radius: var(--radius-sm);
    font-family: var(--font-mono);
    font-size: 0.85rem;
    margin: 0.25rem 0;
  }

  .metrics-section {
    padding: var(--section-spacing) 0;
    background: linear-gradient(
      135deg,
      rgba(var(--color-primary), 0.05) 0%,
      rgba(var(--color-accent), 0.03) 100%
    );
  }

  .cta-section {
    padding: var(--section-spacing) 0;
    text-align: center;
  }
  .cta-content {
    max-width: 800px;
    margin: 0 auto;
  }
  .cta-content h2 {
    font-family: var(--font-heading);
    font-size: 2.5rem;
    color: rgb(var(--color-text));
    margin-bottom: 1.5rem;
  }
  .cta-content p {
    font-size: 1.25rem;
    color: rgb(var(--color-text-muted));
    margin-bottom: 2.5rem;
    line-height: 1.7;
  }
  .cta-buttons {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  @media (max-width: 1024px) {
    .overview-grid {
      grid-template-columns: 1fr;
    }
    .features-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    .stack-grid {
      grid-template-columns: 1fr;
    }
    .modes-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 1024px) {
    .demo-content {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
    .demo-image-wrapper {
      order: -1;
    }
  }

  @media (max-width: 768px) {
    :root {
      --section-spacing: 4rem;
    }
    .project-hero {
      padding: 3rem 0 4rem;
    }
    .demo-content {
      padding: 2rem;
    }
    .features-grid {
      grid-template-columns: 1fr;
    }
    .cta-buttons {
      flex-direction: column;
      align-items: center;
    }
  }
</style>
